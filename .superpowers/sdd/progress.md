# Phase 4 SDD progress ledger — plan docs/superpowers/plans/2026-08-04-phase-4-certs-shipping.md, branch phase-4-certs-shipping, started 2026-08-04
Spec: docs/superpowers/specs/2026-08-04-phase-4-certs-shipping-design.md (18 owner rulings + 4 samples-driven amendments §3.19-§3.22)
Branch base (merge-base main): 586a569. Docs commits before Task 1: d06f94e spec, 27ac8a6 samples, a568767 spec revision, c5585bf plan.
Task 1: complete (commits c5585bf..75e3b5e, review clean — spec ✅, quality Approved; 1018 tests)
  Closed issue #34. PDF transcriptions verified word-for-word by the reviewer, incl. the owner's own "AMERICAN HEAT TREAT" vs "TREATING" inconsistency preserved deliberately.
  GAP FOUND (folded into T12, plan amended): admin/settings page has no boolean- or enum-aware save path, so cert_required_default and cert_scope_default are unusable from the UI as shipped.
Task 2: complete (commits 8b30e7e..a4e1a08, review Approved — spec ✅; 1029 tests; both DBs at 18 migrations)
  Migration SPLIT into two dirs deliberately — Postgres refuses to use a new enum value in the txn that added it (probed, verbatim error in the file header). Do not merge them back.
  Spec §5.5 AMENDED from this review: removing an order from a shipment is refused once its ticket has printed (ShipperOrder has no deletedAt, so removal frees a sequence a customer may already hold on paper). Folded into T9 with tests.
  Minors folded forward: T3 gets the missing SHIPPER-with-orderId-no-shipperId CHECK case + a SNAPSHOT_INCLUDE validity smoke test; T8 gets SNAPSHOT_INCLUDE.shipper's customer select (spec §7 divergence); T9 gets ShipperOrder's other two unique assertions.
  Minors carried (no action): report said 26 CREATE INDEX, file has 27; report called SNAPSHOT_SELECT.storedDocument compile-forced when it was merely correct; traveler.ts toMeta 404 guard is a placeholder until T3 owns the widened shape.
Task 3: complete (commits 1367875..a332b7e incl fix round 1, re-review Approved; 1058 tests; traveler.test.ts byte-identical throughout)
  Fix round closed: (1) friendly download filenames regressed to raw cuids on EVERY document fetch — no test asserted Content-Disposition; six .toBe() assertions added across all four kinds. (2) the union widened GET /api/orders/[id]/documents' authorization surface.
  OWNER RULINGS 2026-08-04 (recorded in spec §8 and §10.2): document lists are permission-filtered per kind using search.ts's globalSearch shape (can() per group, empty the group not 403 the call); and a BOL belongs to exactly ONE shipment and does not exist until printed — independent-BOL and BOL-over-several-shipments both put to the owner and rejected.
  Reviewer verified the fix matches the search.ts precedent structurally, not just behaviourally; AREA_FOR_KIND consolidated into one exported map (better than the Minor asked for).
  Minor carried: round-2 report has GREEN evidence but no RED for the two new test blocks.
Task 4: complete (commits a332b7e..c84421c incl TWO fix rounds, re-review Approved — "the loop converged"; 1074 tests)
  Important: resolveCertSettings' getSetting calls read on a SECOND connection inside createOrder's Serializable txn — the pool-starvation shape printTraveler was fixed for in P3 (R4 finding 8), three lines below createOrder's own warning comment. Implementer flagged it themselves; reviewer judged it a real defect, not an acceptable trade-off.
  CONTROLLER DECISION: this was my plan step's fault ("plant settings read via getSetting"), not an implementation slip — plan corrected in bbc6228, getSetting gains an optional db param, resolveCertSettings routes every read through the db it already receives (readTravelerData is the shape).
  Minors in the same round: customerContainerId .default("") vs the brief's .optional(); no audit-content assertion for the new fields; resolver re-queries the customer row saveNewOrder already holds.
OWNER DECISION 2026-08-04 (parallelism): stay SEQUENTIAL through Tasks 5-12 (the service chain is linear — 4->5->6 inside certs.ts, 8->9->10 inside shippers.ts, and T8 needs T5's createCert). Build a second lane BEFORE Tasks 13-17, where four of the five screens are genuinely independent.
  Second-lane rig when it is built: create+migrate erp_test2; tests/helpers/setup.ts already reads DATABASE_URL_TEST, so the override is per-agent env. Each parallel implementer needs its own git worktree (shared tree = one agent's git add sweeps the other's work) plus its own npm install + prisma generate. NOT a memory constraint — truncateAll() TRUNCATEs every table in beforeEach, so two runs on one DB destroy each other's fixtures (recorded project memory).
  Task 4 round 2: the round-1 Minor fix (customerContainerId .default("") -> .optional()) INTRODUCED an Important bug — auditPayload wrote the field with no ?? fallback, and redact()'s JSON.parse(JSON.stringify()) drops undefined keys, so the create snapshot silently lost the key while every DB-backed snapshot carried "". The 2C-3 "half the findings are in code written for the previous round" pattern, first instance this phase. Fixed with ?? "" + an Object.hasOwn key-presence test (toMatchObject had been passing on the missing key). Reviewer independently re-derived the sweep: customerContainerId was the only affected field.
  Minor carried: report cited "no DeprecationWarning appeared" as evidence reads are sequential — non-discriminating, since tests/helpers/setup.ts suppresses that exact warning class at the source. Sequentiality confirmed by reading code instead.
Task 5: complete (commits c84421c..7dcd3ec incl THREE fix rounds, re-review Approved; 1108 tests, 34 in certs.test.ts)
  Reviewer INDEPENDENTLY wrote+ran a throwaway concurrency test and confirmed the per-scope uniqueness invariant genuinely holds under claimOrder (one success, one clean 409, one live row) — then removed it. Fix round adds it properly as a regression proof (house convention: every row-lock invariant carries a concurrency test — order-loads.test.ts:289, part-process-steps.test.ts:903, orders.test.ts:432).
  CONTROLLER DECISION (plan-mandated circular import): certs.ts <-> orders.ts is a REAL bidirectional cycle, safe today only because every crossing export is a hoisted function declaration. Deferred to T7 rather than fixed here — T7 adds claimOrdersInOrder and T8 needs both createCert and the claims, so one extraction covers all of it. Plan amended in 53787c8: NEW leaf src/server/order-locks.ts holds claimOrder + claimOrdersInOrder, every caller re-pointed, no new tests (moved function, existing suites are the proof). Precedent: Phase 2A's zero-import errors.ts.
  CARRY TO T8 REVIEW: Cert.shipperId has no assertRefExists (correct — that helper is the reference-kind pattern only, and spec §7 omits shipper), but Shipper is soft-deletable, so a raw P2003 catches a nonexistent id and NOT a voided one. T8 must guarantee its call path never hands createCert a voided shipperId.
  Minor: updateCert's voided-cert 404 branch untested (voidCert has the equivalent).
  T5 rounds 2-3 were ONE test. Round 2's concurrency proof passed with claimOrder DELETED — both sides ran Serializable, so Postgres SSI caught the anomaly on the Cert predicate and the Order row lock never did anything. Reviewer found it by regression-and-revert, not by reading.
  FIX (durable technique, reuse it): make the COMPETING caller Read Committed. SSI is then off the table on that side, so only the row lock can serialise the two — CLAUDE.md's "the row lock works at ANY caller isolation", the 2C-3 lesson. Verified RED (a genuinely duplicated live cert row, count=2) then GREEN, by both implementer and reviewer independently. Synchronisation is a real happens-before edge (holder signals only after its SELECT ... FOR UPDATE is awaited), NOT a sleep or timing window.
  The old both-Serializable test is KEPT but retitled and commented as explicitly NOT a row-lock regression guard — honest limited coverage beats a mislabelled proof.
  LESSON FOR EVERY REMAINING RACE TEST THIS PHASE (T7 claim-ordering, T8 numbering/idempotency): a concurrency test must be verified by deleting the lock and watching it go red. Passing is not evidence.
Task 6: complete (commits 7dcd3ec..f4d6839 incl fix round 1, re-review Approved; 1136 tests)
  Seeding/freeze/pass-fail/audit-content all verified correct. Reviewer confirmed the seed-ORDER-BY test genuinely tests sorting (fixtures inserted out of order) and the freeze test mutates the source PartInspection after seeding.
  CONTROLLER DECISION (contract ambiguity, implementer disclosed it and declined to guess — correctly): the BUILT behaviour is right, the NAME was wrong. replaceResults -> replaceReadings, renamed in code+plan+spec (da0054f). Merge semantics are deliberate: readings replaced only under requirements the payload NAMES; unmentioned requirements untouched. A full-wipe contract would make a partial grid submit silently destroy readings. 2C-3 "keep only what the user typed" is the precedent. Locking test lands with the fix.
  CARRIED TO WHOLE-BRANCH REVIEW (systemic, fix once — do NOT single out one task): replaceReadings, updateCert and voidCert all take claimCertsOrder and NONE has a concurrency test; only createCert does. If added, the interleaving worth catching is replaceReadings racing voidCert on the same cert, competing caller pinned to Read Committed (the T5 technique).
  Minor carried: note field's max(500) is an invented cap with no spec basis (harmless, disclosed).
  Rename verified zero-straggler (repo-wide grep for replaceResults returns nothing). Merge-vs-wipe test verified discriminating by the implementer widening the delete to cert-scope and catching RED.
TASKS 4-6 DONE — certification services complete. Next: shipping services (T7-T10).
Task 7: complete (commits f4d6839..590159e incl follow-up round, re-review Approved; 1151 tests)
  Verified: shipped-to-date excludes voided shipments; status derivation reads ONLY lineComplete (tested both directions — full qty uncomplete stays PARTIAL_SHIPPED, one piece complete goes SHIPPED); sequence aggregates with NO live filter so a void never frees a printed number; claimOrdersInOrder is one ORDER BY ... FOR UPDATE statement (EXPLAIN shows LockRows above Sort — a structural argument, not "it worked"); orders.ts <-> certs.ts cycle genuinely GONE.
  Follow-up round takes: (a) the SECOND cycle, cert-results.ts <-> certs.ts, same hoisted-function-only shape — moving claimCertsOrder to order-locks.ts and readCertDetail to cert-results.ts makes it one-directional, done NOW so T8 doesn't need an emergency extraction; (b) deadlock test disclaimer moves into the TITLE (house precedent certs.test.ts:187) since a test-run summary otherwise shows a clean pass; (c) stale "claimOrder (orders.ts)" doc refs in attachments.ts and traveler.ts.
  Reviewer CORRECTED the implementer: the reported context.ts->sessions.ts cycle is a FALSE POSITIVE — `import type` is erased at compile time, so no runtime edge. Their cycle detector doesn't distinguish type-only imports.
  Minor carried: no audit-content assertion on a recomputeOrderStatus status change (scope-appropriate here; a later task exercising the shipment path end-to-end should cover it).
  BOTH module cycles now gone. Reviewer wrote its OWN import-graph detector (handling `import type` and multi-line imports) and ran it fresh: zero cycles in src/server. Leaf modules are now errors.ts and order-locks.ts.
  Deadlock test retitled to carry its own disclaimer (visible in a bare test-run summary, certs.test.ts:187 precedent).
SHIPPING SERVICES: T7 done. Next T8 (createShipper) -> T9 (children) -> T10 (void + order edit invariants).
Task 8: complete (commits 590159e..f740810 incl fix round 1, re-review Approved; 1171 tests)
  Reviewer verified structurally (not by argument) that a voided shipperId CANNOT reach createCert: grep shows exactly two call sites, and shippers.ts passes its own uncommitted in-transaction row. Also confirmed the two subtle orderings are right — priorShipped read BEFORE the insert (over-ship compares against what was already live) and the missing-cert scan AFTER cert creation (this save's own cert is never reported missing).
  TIME BOMB CAUGHT: shippers.ts imports the CONST INT4_MAX from orders.ts and consumes it at module-evaluation time in top-level zod schemas. Task 10 adds orders.ts -> shippers.ts (shipmentBlockers), which would put INT4_MAX in the TDZ and crash at import. Fix round moves it to the client-safe leaf src/lib/order-constants.ts. (Hoisted functions are safe across a cycle; a const is not — that is the distinction that matters.)
  Also in the round: SNAPSHOT_INCLUDE.shipper still lacks a shipper-level customer select (order-less shipments render a blank once T10's removeOrderFromShipper lands); a duplicate child id inside one order produces "A shipper with that shipper number already exists" — a refusal naming a problem that does not exist (orders.ts duplicateSerialError is the precedent); deadlock probe re-run without its 50ms amplification; comments max 2000 -> 4000 to match every other long free-text field; unused test fixtures silenced with `void`.
  PINNED FORWARD (03211b5): T9 asserts SNAPSHOT_INCLUDE.shipper CONTENT — it is the include's first real consumer, since auditedCreate writes a hand-built payload and never reads it. T11 closes createCert's shipperId liveness gap at BOTH the route (no client-supplied shipperId) and the service (findFirst deletedAt: null on the SHIPMENT branch).
  TDZ time bomb ELIMINATED: INT4_MAX now lives in src/lib/order-constants.ts, verified a genuine ZERO-import leaf; all three consumers re-point; no new evaluation-time edge. T10's orders.ts -> shippers.ts edge can now land safely. The one remaining binding shippers.ts takes from orders.ts is isDuplicateClientRequestId, a hoisted function — safe in either evaluation order.
  KNOWN LIMITATION, RECORDED ONCE — DO NOT SPEND MORE ROUNDS ON IT (reviewer's definitive ruling, and it retracted its own round-1 prediction on the evidence): **ABBA deadlock cannot be discriminated by a timing test at this layer.** claimOrdersInOrder is a single SELECT ... ORDER BY "id" FOR UPDATE with LockRows ABOVE Sort, so the CORRECT implementation also holds A while blocking on B — a held-B/request-A probe deadlocks sorted and unsorted alike (false RED, not a discriminator). ABBA needs a window BETWEEN two statements; the sorted version has none, and only an injected delay widens the unsorted one's. No seam exists in createShipper to open one without shaping production code to tests. Both deadlock tests (ship-ledger.test.ts:290, shippers.test.ts:328) are therefore honest production-shape smoke tests with the disclaimer IN THE TITLE. Deadlock-freedom of the claim itself is proven one level down in ship-ledger.
  What IS deterministically testable is the call site — folded into T9 Step 5a: vi.mock("@/server/order-locks") (module boundary, house-legal; NOT a Prisma delegate spy) asserting the FIRST lock call is claimOrdersInOrder with every affected order id. certs.ts also calls claimOrder, hence "first call" not "never called".
Task 9: complete (commits 4cef461..95d9d7d incl 2 fix commits, re-review Approved; 1214 tests, 43 in shipper-children)
  Verified good: the 2nd commit's race fix is COMPLETE (addOrderToShipper now reads position/duplicate state AFTER the claim) and no other mutator has that shape; voided read-only; §5.5 printed-ticket refusal incl. whole-set print covering every order; two-phase negative-park renumber; Step 5a composition tests use a module-boundary vi.mock and assert .mock.calls[0] with invocationCallOrder (no Prisma delegate spies anywhere); Step 5b asserts real snapshot CONTENT (customer.code, carrier.name, shipToAddress.name); Step 6 both uniques exercised as DB behaviour.
  Important: §4.2's "at least one positive-qty line, service-enforced" is unenforced on EVERY edit path. Implementer disclosed the replaceShipperLines case; reviewer found the starker one — removeOrderFromShipper can drop a shipment to ZERO orders, and T9's own test (one-order fixture, "recomputes status when an order is removed") exercises exactly that and passes. Fix enforces at the DOCUMENT level in both mutators (a single qty=0 line stays legal — the invariant is about the shipment as a whole) and the message names the real remedy: void the shipment, which keeps every sequence claimed.
  PINNED TO T11 (95c7428): Task 9's six mutators return a bare ShipperDetail with warnings only via a separate pure overshipWarnings(detail) that NOTHING obliges a caller to invoke — diverging from createShipper's {shipper, warnings, deduped}. A route written as `json(await replaceShipperLines(...))` silently drops the §5.7 warning. T11 wraps every mutating shipment route in the same shape and tests it through the route.
  Minor: audit.ts:136-137 comment credits removeOrderFromShipper to "Task 10"; T9 implements it. TDD ordering deviation self-disclosed (impl before test, RED verified retroactively via git stash — genuine TypeErrors, not assertion failures).
  §4.2 non-empty-shipment now enforced at DOCUMENT level on both reachable edit paths, and the reviewer hand-traced the write-skew case (two concurrent replaceShipperLines each zeroing a different order): both read the same full order set and claim it via claimOrdersInOrder, so the second blocks on the identical FOR UPDATE and Postgres raises a serialization failure at the claim — clean retry, no stale read. No new race.
  A single qty=0 + lineComplete=true line stays LEGAL (the "we're not sending the last three, close the line" case); the invariant is about the shipment as a whole. Tested in both directions.
Task 10: complete (commits 95d9d7d..dfd9282 incl fix round 1, re-review Approved; 1230 tests)
  Verified good: the new orders.ts -> shippers.ts edge is SAFE — reviewer independently reran both load-order tsx probes; the only two crossing exports (isDuplicateClientRequestId, shipmentBlockers) are hoisted functions used only in function bodies, and `import type { Blocker }` is erased. assertPrintable extraction preserves printTraveler's behaviour with the old VOIDED literal fully swept. ship-ledger fixture edit does NOT erode coverage (it voids the shipper via raw prisma to bypass recompute, and the test's actual claim is untouched).
  Important, reproduced EMPIRICALLY by the reviewer: removeOrderFromShipper hard-deletes the join row but leaves that order's SHIPMENT-scope cert pointing at the shipper. voidShipper then computes orderIds from the shipment's CURRENT orders, claims only those, and soft-deletes every cert with that shipperId — writing to a removed order's cert with no FOR UPDATE claim on it. The code's own comment reasoned about orders ADDED later (which create no cert) and never about orders REMOVED later, which is the reachable case.
  CONTROLLER DECISION: fix at the SOURCE, not by widening voidShipper's claim — removeOrderFromShipper voids that order's shipment-scoped cert at removal time under the claim it already holds. A cert scoped to a shipment that no longer carries its parts is orphaned regardless of locking. Recorded as a dated spec §5.6 amendment (17490ca); voidShipper's cascade then becomes provably covered by its own claim.
  Reviewer REBUILT its round-1 scratch repro against the fixed code: the cert is now voided at removal time and voidShipper leaves that deletedAt untouched. Also grep-verified the new comment's premise structurally (removeOrderFromShipper is the ONLY place a ShipperOrder row is ever deleted), rather than accepting a second plausible-sounding justification after the first was wrong.
TASKS 7-10 DONE — shipping services complete. 1230 tests. Next: T11 routes (+2 pinned items), T12 admin UI.
Task 11: complete (commits dfd9282..3ae5371 incl fix round 1, re-review Approved; 1249 tests)
  Both carried items closed on BOTH sides with tests that fail if either half is missing: createCert's shipperId liveness (route .strict() with no shipperId field AND createCertInTx's findFirst deletedAt:null, tested by calling the service directly after voiding); and all six mutators wrapped in {shipper, warnings} with an over-ship warning proven THROUGH the route.
  Fix round: wrap GET /api/shippers/[id] in shipperResponse too. Reviewer's deciding argument — client components cannot import src/server/**, so Task 14's screen has NO seam to compute the warning itself without a second round trip or duplicating the threshold in src/lib. This is the issue #41 shape (P3: "show the printed-traveler warning when the order hub LOADS, not only after a mutation"); closing it at near-zero cost beats rediscovering it.
  Reviewer independently verified permissions-sweep would still pass (regex matches all 18 new handlers) rather than assuming.
  GET now wrapped; the new test over-ships via PUT in one session then GETs with a DIFFERENT session and no write, proving the cross-request property a shape-only test would miss.
Task 12: complete (commits 3ae5371..893dc5e incl fix round 1, re-review Approved; 1270 tests)
  Brief's own asks all correct: type-correct SUBMISSION (coerceForSubmit, verified by capturing the literal fetch body), upload discipline mirroring attachments.ts, SNAPSHOT_SELECT.user excludes signatureImage with .not.toHaveProperty + byte-marker-absent assertions, §5.16 gating.
  SIBLING-SPLIT AGAIN (3rd time this project — 2C-3 counted six, P3's bulk-grid repeated it): the implementer fixed listUsers()'s blanket include, FILED activeManageUsersHolders() as a follow-up, and MISSED getSessionUser entirely — which runs from handle() on EVERY authenticated request and now drags signature bytes through it. This task is what makes that column non-empty. Fix round sweeps the whole group ("queries that load a User row"), incl. authenticateUser's login-time findUnique.
  Browser-verification concern ADJUDICATED SOUND by the reviewer: native-setter + dispatchEvent is what Testing Library's fireEvent uses internally (React listens for real DOM events, isTrusted-independent), and the fetch interceptor captured the app's own JSON body then round-tripped it through a real GET — evidence about the real path, not a simulation. The implementer had also empirically discarded a naive property-set that did NOT fire onChange.
  Minor carried: no jsdom/RTL infrastructure anywhere in the repo (vitest is environment:node, zero .tsx test files), so DOM->save() wiring is covered by manual browser verification only, not by CI.
  Sibling sweep verified COMPLETE by the reviewer's own independent grep (7 read sites + one relation-include + one seed write, census matched exactly). Every narrowed select traced against its actual consumers — no field gaps. getSignature correctly left alone (it exists to read the bytes); seed.ts correctly excluded (one-time bootstrap, return value discarded).
TASKS 11-12 DONE. 1270 tests. Next: the UI block (T13-T17) — SECOND LANE now being built per the owner's decision.
SECOND LANE BUILT 2026-08-04 (owner-approved for the UI block):
  - erp_test2 created + migrated (18 migrations).
  - Worktree /home/cojoa13/Desktop/HeatSynQ-laneB on NEW branch phase-4-lane-b (a worktree cannot check out a branch already checked out elsewhere — each lane needs its own branch, and they merge at the end).
  - Lane B has its own node_modules, its own prisma/generated, and .env with DATABASE_URL_TEST -> erp_test2.
  - ISOLATION PROVEN, not assumed: wrote a probe row into erp_test, ran Lane B's suite (whose beforeEach TRUNCATEs every table), probe survived. Lanes are genuinely independent.
  - Lane assignment: A = T13 shipping list -> T14 shipment page (main worktree). B = T15 cert worklist -> T16 cert detail (lane-b worktree). T17 order-hub runs SOLO after both merge.
  - Shell.tsx is the ONE shared file: Lane A's T13 adds BOTH nav entries so Lane B never touches it. /certs 404s until Lane B lands — the same state every future-phase nav entry has had since Phase 1.
  - Lane B's dev server runs on PORT=3001. Both lanes' dev servers share the DEV db (erp) — fixtures must be distinctly named and cleaned up.
Tasks 13 + 15: dispatched in parallel from base 893dc5e.
Task 13 (LANE A): complete (commits 893dc5e..e6af10d, review Approved FIRST PASS — no fix round; 1278 tests)
  Shell.tsx needed NO edit: reviewer verified by git blame that both the Shipping and Certifications nav entries AND their permission areas exist in Shell.tsx's and permission-constants.ts's FIRST commits (2026-07-29, Phase 1). The one file I planned around as a merge hazard is untouched by either lane.
  Implementer disclosed no-RED-phase for the service test (listShippers was built in T9); reviewer checked and found the new file adds GENUINELY new coverage T9 never had (ship-date range with from/to/both, search by packing-list no / BOL no / order no, plus a negative cross-customer check) — real incremental coverage, not a re-skin.
  Minor carried: brief asked for a demo screenshot, none referenced (likely T20's, but say so rather than omit silently); totalWeight/freightAmount render unformatted, consistent with the orders board but closer to money.
Task 15 (LANE B, branch phase-4-lane-b): fix round in flight (commits 893dc5e..d4f80e6, review Needs fixes, 1 Important; 1274 tests in lane B)
  Verified good: all four non-negotiables (useLatest both paths, no soft-catch, §5.16 gating, no server import); new test file is REAL incremental coverage (reviewer checked certs.test.ts — the pre-existing block never combines filters, never asserts sequence, only ever tests failCount===0); the disclosed browser-tab drift onto lane A's port left NO trace in the diff (Shell.tsx and certs.ts untouched).
  Important: the worklist's pass/fail column derives passed as readingCount - failCount, but an unfilled reading has passed === null, not false. So a cert MID-ENTRY (some requirements filled and passing, rest blank, zero failures) renders as "N passed" — overstating completeness on the page quality lives in. The normal pre-completion state of every cert, not an edge case. Fix distinguishes three states: passed (true) / failed (false) / pending (null), adding a passed-count to listCerts if needed.
  Minor: src/app/certs/page.tsx cites a "customers/parts precedent" for the Server-Component wrapper split that DOES NOT EXIST — reviewer checked every page.tsx and found each is itself "use client" with the component inline. The split is fine (the brief specified two files); the comment misattributes. Correct it so a later reader doesn't hunt for a missing pattern.
MACHINE MOVE 2026-08-04: handoff written into docs/HANDOFF.md §4a + §9 (commit 8729dbe); .superpowers/sdd/ now TRACKED (4a3cb6b) so this ledger and every report/verdict travels. BOTH BRANCHES STILL NEED PUSHING — git push was blocked by the permission classifier and is the only thing not yet protected.
OWNER RULING 2026-08-04 (merge discipline): do NOT merge phase-4-lane-b into phase-4-certs-shipping until the WHOLE BRANCH is pushed and reviewed. The lane folds in as part of the finish sequence (after every task is done and the whole-branch review has run), never as a convenience step at the start of a session. HANDOFF §4a and §9 corrected — they had said "merge it first thing", which is now wrong.
Task 15 (LANE B): complete (commits 893dc5e..3acd9b5 incl fix round 1, re-review Approved; 1275 tests in lane B)
  Fix distinguishes three states explicitly (passed===true / failed===false / pending===null) with a server-side passedCount alongside failCount, rather than inferring passed by subtraction. Reviewer hand-traced every combination AND wrote its own throwaway test against the real erp_test2 DB for the two combinations neither shipped test covered (all-three-states-on-one-requirement, and all-pending) — both held.
  Also fixed: src/app/certs/page.tsx's comment no longer cites a customers/parts precedent that does not exist.
LANE B GAP FOUND AND CLOSED at handoff time: phase-4-lane-b branched from 893dc5e, BEFORE the phase branch un-ignored .superpowers/sdd/ in 4a3cb6b — so Task 15's brief, report and reviewer findings were still local-only on that branch and would have died with the laptop. Brought the two .gitignore rules over and committed lane B's three markdown files (e56cc91, pushed). Watch for this shape on any future branch cut before 4a3cb6b.
BOTH BRANCHES PUSHED to origin 2026-08-04: phase-4-certs-shipping @ c2e08be, phase-4-lane-b @ e56cc91. Verified on the remote: 0 unpushed commits, 97 record files, 5 sample PDFs.
Task 14 (LANE A): LANDED UNREVIEWED (commit e54684c, pushed). Owner called the machine move first and will review it after the reformat. 1279 tests, gates clean, browser-verified end to end (two-order shipment, both orders flipped to Shipped, void locked every control — checked disabled AND readOnly, not just .disabled).
  TO ADJUDICATE AT REVIEW: it added GET /api/shippers/[id]/documents, a route NOT in spec §9's table. Almost certainly a genuine gap (T3 built listDocumentsForShipper, T11 built the ORDERS equivalent, nothing ever exposed the shipment one, and the page needs it for its stored-documents list) — but a route outside the spec's table gets adjudicated, not waved through.
PLAN HOLE FOUND BY TASK 14'S IMPLEMENTER — Task 14b added to the plan 2026-08-05:
  There is NO shipment creation flow anywhere in the 20-task plan. createShipper exists (T8), POST /api/shippers exists (T11), but NO SCREEN CALLS EITHER. Consequences: (a) the credit-hold gate — this phase's headline feature, the blocking half of owner ruling §3.7 — is UNREACHABLE by a human, since it fires only in createShipper and the edit page never calls it, so the banner T14 was asked to render cannot appear in the running app; (b) clientRequestId has no producer, so T8's idempotency protection is never exercised in practice; (c) THREE of T20's five E2E flows assume the literal route /shipping/new and cannot run.
  The implementer refused to invent it unilaterally — correct, since its shape (one atomic nested POST allocating the packing-list number, every order's sequence and the SHIPMENT-scope certs in one transaction) is architecturally unlike the edit page's model, and "create empty then edit" would burn a number per abandoned draft and defeat the nonce. Task 14b is now written into the plan with that shape; build it BEFORE T20.
NEW MACHINE 2026-08-05 (cjones): §8 setup done, all four gates green pre-work (1279 tests, tsc, eslint, build). erp_test2 + lane-b worktree REBUILT (../HeatSynQ-laneB, branch phase-4-lane-b @ e56cc91, DATABASE_URL_TEST -> erp_test2, smoke green).
Task 14 RETRO REVIEW (review-task-14.diff, e593d79..e54684c; full text in task-14-review.md): Spec ❌, quality Needs fixes — 0 Critical, 1 Important, 4 Minor. Implementer report was LOST in the machine move (never committed); task-14-report.md is a labeled controller-written stand-in from the ledger + commit message.
  ADJUDICATION A ACCEPTED: GET /api/shippers/[id]/documents is a faithful gap-fill — §8 per-kind filter satisfied BY CONSTRUCTION (ownerColumns nulls shipperId for TRAVELER/CERT behind the DB CHECK; both reachable kinds map to shipping in AREA_FOR_KIND). Recorded as a dated spec §9 amendment (route table row + rationale), same-day.
  ADJUDICATION B: voided state provably locks every control in code (disabled on selects/dates/checkboxes, readOnly on text — correct per control type). The browser claim itself stays uncorroborated.
  Important #1 -> fix round 1: added grid lines prefill ship-now to FULL ordered qty/weight, not ordered − shippedToDate (ShipmentOrderPanel comment self-acknowledges: no client-callable shipped-to-date for candidate lines). Over-ship default the operator only discovers post-save via §5.7.
  Minors deferred to whole-branch review: §5.16 titles missing on state-disabled (not-dirty/nothing-picked) buttons across all three grids + Add order; add-order candidates fetch lacks a useLatest ticket; documents route test lacks the 404 case (+ optional no-leak pin); voided banner's "see History" fallback useless without admin.view.
  ⚠️ residuals resolved by controller: browser screenshots — none exist in the repo; treat as not-produced, T20's demo walkthrough re-captures. TDD RED evidence — unprovable post hoc, noted here rather than re-litigated.
Task 14: fix round 1/5 (1 addressed, 0 open — remainder prefill; commit efde514, re-review Approved via review-task-14-fix1.diff)
  Fix verified by the re-reviewer at every layer: widened id set is a genuine superset drawn per-order (order.lines under the already-joined relation, still ONE shippedTotals call); voided exclusion rides ship-ledger's shipper.deletedAt filter and the test proves a PRESENT 0/0 entry (toMatchObject fails on undefined); both addPicked AND addAllRemaining prefill via the pure shipRemainder (max(0, ordered−shipped), 2dp, floor-at-zero); §5.7 untouched — default, not a cap. Sibling enumeration accepted: lines changed; containers (no ledger exists) and serials (no quantity) deliberately not, each with a SIBLING-SPLIT NOTE in code.
Task 14: complete (commits e593d79..efde514 incl retro review + fix round 1, re-review Approved; 1286 tests)
  Residual carried to T20: browser verification of the shipment page (original claim uncorroborated, fix round explicitly not browser-verified) — T20's E2E flows and demo walkthrough are the honest recapture point. ShipperOrderDetail grew orderLineShippedToDate[] — include in the whole-branch §9/§11 sweep alongside the documents route.
Task 16 (LANE B): complete (commits e56cc91..a749f30 + review-filing commit, review Approved FIRST PASS — spec ✅; 1276 tests in lane B; review in lane B's task-16-review.md)
  Second first-pass approval this phase (T13 was the first). Browser verification genuinely performed (Playwright): pass/fail computed live, override round-tripped, post-print lock proven with a purpose-made certs.edit-only user, void lockdown per control type.
  ADJUDICATION ACCEPTED: GET /api/certs/[id]/documents mirrors the shipper documents route — §8 filter satisfied by construction (CHECK: certId non-null only for kind CERT; AREA_FOR_KIND maps CERT -> certs). Spec §9 amended on MAIN same-day (lane B's spec copy diverges until fold-in — expected, do not "fix" the lane).
  T15's three-state lesson verified honored structurally (explicit === true/false/null at every render site); client DECIMAL_10_4 regex byte-identical to decimalField(10,4); computePassed genuinely shared.
  FIX-WAVE CANDIDATE FOR WHOLE-BRANCH REVIEW (cross-page sibling group, fix all three together, never one): the notes-pair optimistic-PATCH clobber — a save of one field in flight can reset text typed in the sibling — exists byte-for-byte on CertDetail, ShipmentDetail, and customers. Reproduced live during T16's verification.
  Minors deferred: two ungated effects (documents list, voidReason — benign under remount-per-id); rollback-load-failure keeps optimistic text; malformed reading value shows Pending until save; trailing-zero display; HistoryPanel staleness after same-page saves.
Task 17 (LANE B): dispatched after T16; FALSE START — controller extracted the brief by LINE NUMBERS from the lane's plan copy, which is stale (cut before Task 14b shifted the plan), yielding Task 18 code-fragment garbage. Implementer correctly BLOCKED instead of improvising from the dispatch summary (prime directive honored). Brief rewritten from MAIN's plan, agent resumed.
  LESSON (controller-side sibling of the machine-move gap at line 104): any file extracted by position from a doc that exists on BOTH branches must come from the branch where the doc is current. The lane's plans/specs copies are stale by design until fold-in.
Task 14b: complete (commits a78c1a2..2967163, review Approved FIRST PASS — spec ✅; 1288 tests; review in task-14b-review.md)
  THIRD first-pass approval this phase. The credit-hold gate is now human-reachable: browser-verified refused (no-override clerk, §5.16 title names override_credit_hold) AND overridden (blank reason refused, reason lands in the audit entry), plus idempotent replay (deduped: true, same packing-list number).
  ADJUDICATION A (seam contradiction with fix round 1's rejected alternative): RIGHT SEAM — one indexed batched query (@@index orderLineId), and the staleness argument was an edit-page argument (self-inflicted via the page's own saves); on create, drift is only concurrent-shipper, which §5.7's post-save warnings close. NO divergence risk: one derivation (shippedTotals), two transports, each page reads exactly one. Noted: OrderDetail.orderLineShippedToDate rides UNUSED in the edit page's catalog payload — dead weight, whole-branch review may trim or keep.
  ADJUDICATION B (shared-grids refactor of reviewed T14 code): behavior-preserving — useBulkGrid save cycle, both prefill paths, candidate ledger overlay, gate flow-through, SIBLING-SPLIT NOTEs all survive in ShipmentGrids.tsx. The sibling GROUP COLLAPSED into one copy consumed by both pages — better than the rule asked for.
  Minors deferred to whole-branch review: serials prefill over-includes on repeat shipments (no per-serial shipped fact exists; parity with edit page; possible §5.7-warning gap worth an owner question); create-page header inputs not §5.16-gated for non-holders (Save carries the reason, no dead end); Add order/Save-while-saving/Remove order title gaps; warnings list keyed by index.
NEXT ON MAIN LANE: T18 (shipping ticket layout, samples contract). T17 still in flight on lane B.
Task 18: complete (commit 4130260, review Approved FIRST PASS — spec ✅; 1305 tests; review in task-18-review.md; reviewer read the sample PDF itself)
  FOURTH first-pass approval. One ticket per ShipperOrder with per-sheet pageBreak (never MOS — §3.20); print entry is a faithful printTraveler transplant (claim → re-read → assertPrintable → read-on-tx → render → archive in one Serializable tx); reprint Buffer.compare stored-only; template-as-data purity ASSERTED by a JSON round-trip test.
  DEVIATION RULINGS: (a) no signature on ticket = SPEC-FAITHFUL (§3.11/§10.3 attach the signature to the CERT; the sample ticket has only Received By/Date — T19's cert owns it). (b) Page N of M omitted = correct resolution of a real conflict (pure-JSON template cannot carry page-count functions; a render-level footer would number across the multi-ticket PDF) — OWNER PING pinned for the finish: §10.1 lists the field; Phase 7's designer is the natural home. (c) tear-off absolutePosition overlap threshold is REAL at ~8 extra multi-line part rows (not "pathological") — flow-based fallback owned by T19 or Phase 7; cosmetic-only failure.
  Minors deferred: ?cert=0 refused with a message naming the wrong blocker (T19 replaces the branch — T19 MUST fix in passing); no 401 case on the print route; zero-order blank-PDF edge (likely unreachable after T9's §4.2 document-level enforcement — whole-branch review verifies reachability before spending a fix).
  ⚠️ resolved by controller: visual spot-check of a rendered ticket against the sample is pinned to the T20/demo round rather than re-rendered now.
OWNER PINGS ACCUMULATING FOR THE PR/FINISH REPORT: (1) ticket Page-N-of-M omission (spec §10.1 vs template purity); (2) serial re-shipment has no §5.7-class warning and no per-serial shipped fact (14b minor); (3) tear-off overlap past ~8 extra part rows.
NEXT: T19 (BOL + certification layouts) dispatched on main lane. T17 still in flight on lane B.
Task 17 (LANE B): implemented (commits caa79dc..1cc83fb, 5 commits; 1283 tests in lane B; review IN FLIGHT with three adjudications: POST /api/orders cert-override widening vs §9's PATCH-only row; the customerContainerId data-loss fix's completeness; §4.1 live-orphans-only reading).
  IMPLEMENTER FOUND A LATENT T4-ERA BUG: the hub containers grid's whole-array replace silently blanked stored customerContainerId on EVERY save since Task 4 — this task's grid-column addition is what surfaced it. Review verifies fix + sweep.
FOLD-IN HAZARD (recorded early, check at merge): the lanes are NO LONGER disjoint in src/server/shippers.ts — lane B's 15f0252 widened ShipperRow (per-order breakdown) while main's 14b/T18 reworked readShipperDetail and added the print entry. Also both lanes touched src/server/orders.ts (main: orderLineShippedToDate seam; lane B: caa79dc entry-time cert override). Expect real (if small) merge conflicts; re-run full gates after fold-in before trusting anything.
Task 17 (LANE B): complete (commits caa79dc..1cc83fb + review-filing commit, review Approved FIRST PASS — spec ✅; 1283 tests in lane B; review in lane B's task-17-review.md)
  FIFTH first-pass approval. Controller verified the ⚠️ items: 45eb23e carries parts+customers atomically; 1cc83fb carries BOTH container grids; zero trailers.
  ADJUDICATION A ACCEPTED: POST /api/orders widened with optional certRequired/certScope — the ONLY workable implementation of §6.1 "overridable at entry" (§6.2's eager in-transaction cert makes create-then-PATCH unable to suppress or prevent it). Spec §9 amended on MAIN same-day. Parity with PATCH proven (identical shapes, effective-pair audit).
  ADJUDICATION B: the customerContainerId data-loss bug was REAL (hub grid omitted the column; replaceContainers delete-then-createMany defaults ""), fix COMPLETE (both grids; sweep ENUMERATED across OrderContainer/OrderSerial/OrderCharge/Load/ShipperContainer — no other omit-and-blank column exists). No automated regression possible client-side (no component harness) — E2E PIN FOR T20: hub container save without touching Cust Cont Id preserves stored values.
  ADJUDICATION C: live-orphans-only is the correct §4.1 reading (voiding is a named human resolution; voided orphans stay listed, dimmed).
  Minors deferred: override-path audit content asserted only transitively; gap summary counts orphans ("covering" wording); re-selecting the resolved value records a no-op override.
LANE B ASSIGNMENTS ALL DONE (T15, T16, T17). Lane B is idle; remaining work is main-lane (T19 in flight, T20) + the finish sequence.
