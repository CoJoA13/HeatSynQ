import { PlaceholderPage } from "@/components/patterns";

export default function OrdersPage() {
  return <PlaceholderPage title="Orders" note="The Quote → Order → Invoice workflow is built in the next phase." />;
}
