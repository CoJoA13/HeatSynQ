# Phase 6 (Quoting) demo — 2026-08-12

**Run against the dev server with scripted-Chromium screenshots** (the HANDOFF §5a technique),
demo fixtures (customer `DEMO` · Demo Gear & Axle Co., part `DG-4140-PINION` at 12.5 lb each with
a two-step recipe, step codes `AUSQ`/`TEMP`, terms Net 30), purged from the dev DB afterwards.
The owner ruled on the full 8-item ratification queue assembled at merge (HANDOFF §6).

## The walkthrough (what was shown, and what it proved)

1. **Ending statement** created on the reference page with the Default flag — new quotes pick it up.
2. **Quote #1019**: number allocated; dates defaulted (effective today, expiry +30d from
   `quote_valid_days`); RFQ / contact-in-Attn / quoted-qty / default statement all live. Line 1
   priced in the 5A vocabulary (setup $150, $0.85/EACH, $400 minimum, break 1,000 → $0.72);
   line 2 free-text (`DG-9100-HOUSING`, own each-weight) — ruling 1's XOR.
3. **Order entry** showed "Quote link (auto): Quote #1019" BEFORE save, with the re-pick select.
4. **The draft invoice**'s operation row read "Quote #1019" at **$575.00** — 500 × $0.85 = $425
   (beats the $400 floor) + $150 setup, the engine's exact ruling-13 math; the part carries NO
   part-price rows, so the number is structurally tier 1's. (The GL-readiness banner correctly
   flagged the account-less demo step code — 5C's refuse-to-export guard.)
5. **Close with reason**: the amber warn-list named order #1448 with the judged-at-link-time
   explanation; the status line records reason/by/when.
6. **Worklist**: a backdated quote sat in BOTH sections (spec: information, not a bug). **Ruling
   7 live**: creating a second pinion quote while #1019 was open stopped on the created-with-
   warnings panel naming the competing quote and its window — warned, never blocked.
7. **Part page** listed the open quotes covering the part.
8. **The PDF** (stored, byte-exact reprint) matched the sample's structure: Attn block,
   Effective/Expires/Terms/RFQ, the boxed column strip, PRICE section with Plus/Or, the break
   line, ending statement, signature with `User.title` ("Production Manager").

## The rulings (all eight taken 2026-08-12)

| # | Item | Ruling |
|---|---|---|
| 1 | Inactive-customer refused / inactive-part accepted on quote lines | **Ratified as built** (the 2C inactive-vs-deleted reading; pickers hide inactive parts anyway) |
| 2 | CLOSED quotes still block part/customer deletes | **Ratified as built** (a closed quote is a kept record; resolution path orders → quote → part/customer) |
| 3 | One-time dormant-column audit churn on first lines save after attach-part | **Accepted as built** (the attach is separately audited; dormant text is superseded) |
| 4 | Grid names every source / PDF annotates QUOTE lines only | **Ratified as built** (grid = §7.5's always-visible; paper stays 5A-sample-faithful) |
| 5 | Part-page active-quotes read gated `orders.view` | **CHANGE: re-gate to `quotes.view`** → issue #101 |
| 6 | Quoted-by picker's options need `manage_users` | **Accepted as built** (quotedBy defaults to the actor; reassignment is admin) |
| 7 | `QuoteLine.eachWeight` at Part's real `Decimal(10,4)` | **Ratified** (spec's in-place correction stands) |
| 8 | The PDF's 9 documented layout deviations | **All accepted**; plus the demo observation — the bare "Material:" label printing when the part has no material — **suppress** → folded into issue #100 |

Rulings 1–4, 6–8 confirm built behavior (no contract change — nothing amends spec §15). The two
change items are issues #101 (gate) and #100 (Material suppression), neither blocking anything.
