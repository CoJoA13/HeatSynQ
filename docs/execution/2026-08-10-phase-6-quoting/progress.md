# Phase 6 — Quoting — Execution Ledger

**Branch:** `phase-6-quoting` · **Spec:** `docs/superpowers/specs/2026-08-10-phase-6-quoting-design.md` (approved 2026-08-10) · **Plan:** `docs/superpowers/plans/2026-08-10-phase-6-quoting.md` (11 tasks)

Committed on the first task per the standing rule (the `.superpowers/sdd/` gitignore-clobber lesson —
git ignore rules only bite untracked paths, so committing early closes the exposure window).

## Design session (2026-08-10)

One-question-at-a-time owner brainstorm; fourteen rulings recorded in the spec's §3. The owner
supplied `docs/samples/Quote_Sample_Form.jpeg` mid-session (VS stock quote form — the PDF build
target) and approved the spec including the seven flagged precedent-based calls (notes pair;
close/reopen under `quotes.edit`; delete-with-reason + §5.14 block; immutable `customerId`;
empty linked quote = needs-price, never part-price fallback; contact delete not blocked;
no attachments this phase).

Code-hook verification done before the brainstorm: `quotes` area exists (`permission-constants.ts`),
`quote_number_next` already seeded in settings (consumed by nothing — Phase 6 only wires it),
NO Quote model / order column / auto-link exists anywhere (the kickoff's "order entry already
auto-links" was the spec's rule, not built code), tier-1's insertion point is the per-line
`PriceRowInput[]` assembly in `invoices.ts` (`listPartPrices` at ~line 401), `PriceSource` and
`DocumentKind` are both DB enums (⇒ two `ADD VALUE`s in one earlier migration directory).

## Task ledger

| Task | Status | Implementer | Review | Notes |
|---|---|---|---|---|
| 1. Schema, migrations, registrations | ✅ DONE | subagent (task-01) | ✅ Approved, Spec ✅ (task-reviewer, round 1 — zero Critical/Important) | Data layer + 2 hand-written migrations on both DBs + all registrations; gates green (126 files / 1952 tests). Reviewer verified the restated CHECK arm-by-arm, the enum split, and that both sweeps were strengthened. Two spec-text deviations for owner ratification (surfaced in controller summary): eachWeight mirrored at Part's real (10,4) — spec corrected in place; QuoteLine.partId blocker deferred to Task 7 (registry is structurally reference-kind-only). endingStatement pulled forward as bare BlockerTarget — Task 2 absorbs. Minor: no-RED-narration note (schema-only task, sanctioned) |
| 2. `endingStatement` reference kind | ✅ DONE | subagent (task-02) | ✅ Approved, Spec ✅ (task-reviewer, round 1 — zero Critical/Important; 4 Minor deferred to whole-branch triage: soft-deleted-row promote 200s silently (inherited generic-service hole, same as terms), a latent Serializable-snapshot precondition worth one comment sentence if a ref kind ever gains an FK column, §5.16 polish on the inactive-row Default checkbox, RED-window note matching the close-periods precedent) | Eleventh kind wired through the generic machinery (constants, EXTRA_SCHEMAS, new "boolean" extra-field kind for the grid/export/paste, picklist by derivation); Task 1's three temporary shims absorbed. At-most-one-live-default enforced in the service under `pg_advisory_xact_lock(4300, 0)` (predicate invariant — the period-lock precedent), demotions audited; concurrency test RED-verified (lock removed from the promote branch → two live defaults survive, competing caller pinned Read Committed). Gates green (126 files / 1967 tests, +15; tsc/eslint/build clean; E2E 18/18 — reference screen changed, fixtures cleaned) |
| 3. Quote service: create/read/list/worklist | ✅ DONE (fix round 1 applied) | subagent (task-03) | ✅ Approved, Spec ✅ (task-reviewer round 2 — both Importants + 2 Minors verified closed; the reviewer confirmed the NOT{} substitution would go red by SQL three-valued-logic analysis. Carried to Task 4: SNAPSHOT_INCLUDE.quote first exercised there; carried to owner: the refuse-inactive-customer / accept-inactive-part asymmetry) | `quotes.ts` (createQuote/getQuote/listQuotes/quoteWorklist/exportQuotes) + `quote-constants.ts`; tests 5 → 43 in quotes.test.ts (fix round: inactive-customer refusal + inactive-part acceptance — asymmetry queued for owner ratification; `followUpDue: false` NULL-date complement RED-checked against a Prisma NOT{} swap; deleted/unknown explicit endingStatementId; expired:false at the =today boundary). Part-prices mirrored (shared decimalField scales; `LOT_WITH_BREAKS` now exported from part-prices.ts and imported, not re-declared). RED-verified: the one-live-line-per-part payload dup (service check removed → the create LANDS both lines; the (quoteLineId, stepCode) partial unique never sees it) and the concurrent create (naive unguarded counter read + Read Committed → both read 1000, loser is a 400 the test refuses; restored → distinct consecutive numbers, losers clean 409s). §5.4 boundaries pinned: followUpDate ≤ today, expiryDate < today, both OPEN+live, one quote in both sections. Gates green post-fix (126 files / 2005 tests; tsc/eslint/build clean; E2E n/a — no UI/route). 10 documented deviations (report §Deviations, deviation 4 corrected in the fix round) — notable: quotedQty×unlimited contradiction 400s, linked-part `active` NOT checked (brief says "live"), derived-filter complements avoid Prisma NOT over nullable followUpDate |
| 4. Quote service: update/close/delete + routes | ✅ DONE | subagent (task-04) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical; 1 Important FOLDED INTO TASK 5 as mandatory work: the §5.14 SSI pairing is recorded only quote-side while orders.ts still says "Serializable for uniformity" — Task 5 must add the load-bearing comment (naming BOTH halves: isolation AND the link writer reading the quote line in the same Serializable tx) plus the dangerous-direction test. 4 Minors: `linesPayloadFrom` omits eachWeight (→ Task 8 brief, silent-null hazard on free-text lines), same-customerId-only payload mints a no-op audit entry (→ triage), one-sentence comment on the spec-sanctioned link-onto-just-closed race (→ Task 5), warn-list SHIPPED/REOPENED literal coverage rows (→ triage). Reviewer independently traced the SSI cycle and the stamp-then-remint vs non-deferrable partial uniques — both sound) | Mutation half of `quotes.ts` (updateQuote/attachPart/closeQuote/reopenQuote/deleteQuote/quoteOrderBlockers, all under a `claimQuote` SELECT…FOR UPDATE) + the full `/api/quotes*` route surface minus print (T10) and eligible (T5). Isolation: update Serializable iff the payload assigns registered FKs (`assignsFk` — the `lines` arm also SSI-pairs the §5.14 OrderLine-predicate guard with the Serializable order save), attach/delete always Serializable, close/reopen claim-only. Array-replace is diff-and-write: kept ids update IN PLACE (`OrderLine.quoteLineId` stays valid — no delete-and-recreate precedent could give that), step-code/threshold re-keys stamp+re-mint (partial-unique swap hazard), stamps before creates. Close warns-and-lists open not-fully-invoiced linked orders (`finalizedInvoicesFor`, never `Order.status`); reopen clears close fields (audit before-snapshot keeps the story); SNAPSHOT_INCLUDE.quote first exercised with content-asserted diffs. RED-verified: close-vs-update race with the claim removed → the edit landed on the CLOSED quote. Gates green (127 files / 2040 tests, +35; tsc/eslint/build clean; E2E n/a — no UI). 9 documented deviations (report) |
| 5. Eligibility leaf + order auto-link | ✅ DONE | subagent (task-05) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important; both MANDATORY halves verified: the load-bearing comment names isolation + the in-tx read, the dangerous-direction test's commit order is barrier-forced so the abort lands deterministically on the save. 4 Minors → triage: judgeQuoteLine's walk should become naming-only with the query as verdict; the SSI test's 200ms sleep can false-RED under load (never false-GREEN); create-vs-update audit shape asymmetry (cosmetic, self-flagged); RED narration complete only for the SSI test) | `quote-links.ts` LEAF (eligibleQuoteLines/resolveAutoLink/judgeQuoteLine — the §5.2 rule ONCE, inclusive both ends, ruling-7 ordering; throws nothing, Prisma types + date lib only) + the three-way `quoteLineId` semantics in createOrder/addLine/updateLine (explicit id judged with line+reason named; explicit null; absent = auto-resolve on create/addLine, KEEP on updateLine — ruling 6) + `GET /api/quotes/eligible` (orders-area view, §5.15 reasoning) + per-line quoteLineId/quoteId/quoteNumber on OrderDetail. MANDATORY items shipped: createOrder's ⚠️ load-bearing-Serializable comment (both halves named + the spec-sanctioned close-race one-liner), the section comment carve-out, and the dangerous-direction test (gated REAL createOrder vs REAL updateQuote line-drop; RED-verified by pinning saveNewOrder to ReadCommitted — both committed, link landed on the soft-deleted line, post-mortem row captured; restored → SSI aborts the save 40001→409). SNAPSHOT_INCLUDE.order extended (lines pull quoteLine→quoteNumber; link edits read "1080 → 1085" in history). linkedOpenOrders left quote-side (sanctioned). Gates green (128 files / 2070 tests, +30; tsc/eslint/build clean; E2E 18/18, fixtures cleaned). 4 documented deviations (report) |
| 6. Tier-1 invoice substitution | ✅ DONE | subagent (task-06) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important. Reviewer RULED the no-source needs-price line spec-supported (tier 3 has no source; the link stays discoverable order-side). 4 Minors → triage: zero-net LEAD-line asymmetry 500s where non-lead skips (throw is the safe direction; report note corrected by the reviewer), sourceQuoteNumber forgeable on the manual-lines save within that surface's existing trust model (cheap .refine suggested), recalculate link-removed/link-added coverage wishes, grid-all-sources vs PDF-QUOTE-only routed to the demo) | Engine pass-through (`PriceRowInput.priceSource`/`sourceQuoteNumber`, OPERATION line emits what the row carries — no math change, existing suite green); both assembly sites branched (`buildPricingInput`'s per-line loop — shared by create AND recalculate — and createInvoiceInTx's lead-price `processNames` path): a linked line's rows come from the quote line's LIVE `QuotePrice`/breaks WHOLESALE (part rows not fetched; empty agreement → needs-price, RED-verified part rows absent), GL/ordering mirroring `listPartPrices` exactly, reads on the invoice tx's own client (#60 lesson; #60 itself untouched). `sourceQuoteNumber` frozen at line write and carried by credit copy, recalculate's manual-line re-create, `LINE_INPUT` round-trip; display reads the frozen column unconditionally — UI grid names every op line's source ("Quote #1006"), PDF annotates QUOTE lines only (5A sample fidelity). Dead/mismatched link = plain Error (corrupt state per §5.14 — never a silent part re-price), fabricated raw + tested. Gates green (129 files / 2084 tests, +14; tsc/eslint/build clean; E2E 18/18 — invoice grid + PDF display changed; fixtures cleaned). 5 documented deviations (report) |
| 7. Cross-entity §5.14 blocks | ✅ DONE | subagent (task-07) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important. Reviewer independently verified BOTH SSI pairings (deletePart↔quote-line writers all Serializable with in-tx reads; deleteCustomer↔createQuote sufficient because customerId is immutable) and ruled the CLOSED-quote-blocks call clearly right under the standing-agreement model (resolution path orders→quote→part/customer, never a deadlock) — still queued for owner ratification. 3 Minors → triage: no dangerous-direction test for the two new guards (the customerId-immutability dependency is load-bearing and untested in that direction — ticket-worthy), RED narration explicit only for the liveWhere exercise, quotedById examined post-review: follows the display-only model like closedById/cert signers — no block, consistent with every document's deleted-creator behavior) | Every quote-blockable delete path now refuses-and-names. `deletePart` + `deleteCustomer` gain in-tx quote-count guards (live = `deletedAt` only — CLOSED blocks, deleted never; counted per quote so refusal and panel agree) with `partQuoteBlockers`/`customerQuoteBlockers` naming each quote the registry way ("Quote · #1006", linked); both blocker routes + Excel exports serve the full union (order+quote / part+order+quote, workbook contents asserted); both detail pages' matchers extended so the existing BlockerPanel renders the new refusals. Step-code (QUOTE_VIA_PRICE) and endingStatement blocks verified behaviorally with zero new code — the Task 1 registry entries carry them; the from-the-grave case pinned at all three chain levels and RED-verified (quote-level liveness dropped from liveWhere → the dead-quote test caught it). Contact deletion pinned NOT blocked through the real `deleteContact` service (coverage was read-side only). Parts-side enforcement documented as blocker-list-IS-the-enforcement (no sweep analogue — registry is structurally reference-kind-only; comments in reference-links.ts + the sweep say a new Part-referencing FK is added by hand). Gates green (129 files / 2094 tests, +10; tsc/eslint/build clean; E2E 18/18 — both delete panels changed; fixtures cleaned). 4 documented deviations (report) — notable: CLOSED-quote-blocks queued for owner ratification |
| 8. `/quotes` UI | ✅ DONE (fix round 1 applied) | subagent (task-08) | ✅ Approved, Spec ✅ (task-reviewer round 2 — the Important closed with refresh-without-adopt, the better mechanism; both Minors in house shapes; scope clean. Owner-triage flags carried: the dormant-column one-time audit churn on first save after attach-part (upstream Task 4 contract), the Task 10 documents-route path obligation, the manage_users options gate). Round 1 was: Needs fixes → fix round 1 closed all 3 (1 Important: refused delete adopt()ed the fresh detail and silently discarded a dirty draft — now the fresh fetch feeds only the blockers panel, form untouched, verified live with a dirtied form + fabricated link; 2 Minor: close/reopen rollback-reload failures now appended not swallowed, add-break empty-draft tooltip). Re-review pending | Ruling 11's two pages as pure client components over the Task 4/5 routes — zero server changes. `/quotes`: the two §5.4 worklist sections (counts; open / bump-follow-up date-picker PATCH / close-with-reason rendering the `linkedOpenOrders` warn-list), New quote section (customer + first line, `router.push` to detail where server defaults surface), house-pattern list (search, derived-Expired status filter via `expired=1`, follow-up-due, customer, 3 date ranges, Excel sharing one `buildQuery`). `/quotes/[id]` (`key={id}` remount): SINGLE-SAVE form — the notes-pair clobber family gets no fourth member — with the PATCH built as a client-side diff in `quote-form.ts` (changed header keys only; `lines` only when the tree changed; **eachWeight carried on free-text lines**, closing Task 4's `linesPayloadFrom` Minor; part-linked lines' dormant columns blank per deviation 5); 5A part-prices card UX for price rows + breaks (basis-change warning verbatim); linked-line indicators proactively lock §5.14 part-change/removal, delete refusal renders BlockerPanel derived from the detail payload + existing blockers/export Excel; close/reopen/delete prompt-reason dialogs; attach-part via the dedicated route (dirty-locked); Documents reads the Task 10 route path (404 → empty state); Print disabled "Printing lands in Task 10"; HistoryPanel. Round-trip no-op verified LIVE against the dev server (untouched load → Save disabled, no request; notes-only edit → `{"notes":…}` alone; break-only edit → lines payload carrying `"eachWeight":"12.5"`, QuoteLine/QuotePrice `updatedAt` byte-identical after save, only the break row written in place). Gates green FRESH (129 files / 2094 tests; tsc/eslint/build clean; **E2E 18/18** — rerun watched to completion after an API-limit cutoff killed the first attempt at flow 18 of 18; dev-DB fixtures verified clean). 6 documented deviations (report) — notable: no `GET /api/quotes/[id]/documents` route exists, section wired to the path **Task 10 must land it at** (the invoices T18→T19 precedent); quotedBy picker options gated `manage_users` (the only users list) |
| 9. Order entry / hub / part page surfaces | ✅ DONE | subagent (task-09) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important; the ABSENT guarantee verified structural through spread semantics, JSON serialization, opaque draft storage, and restore normalization. 3 Minors: the part-page indicator's orders.view gate is safe-but-arguably-wrong-area by the eligible route's OWN §5.15 principle → OWNER TRIAGE (should it read with parts.view/quotes.view?); LineQuoteRepick lacks the sibling's useLatest out-of-order guard (cosmetic, named-400 worst case) → triage; a pure-logic vitest wish on pickOrUndefined → triage) | UI only, zero server changes (none proved necessary). Entry form: `QuoteLinkPicker` per line — auto-link preview off `/api/quotes/eligible` + three-way re-pick select (Auto sentinel / candidates latest-effective-first / No quote); the pick IS the state and the previewed id is never copied into it, so an untouched control sends ABSENT structurally (`buildCreateBody` writes `quoteLineId` only for a non-undefined pick — proven on the wire: untouched save's line keys were exactly partId/qty/weight/serials and the server auto-linked); drafts carry the pick (`LineDraft.quoteLineIdOverride` rides the autosaved payload, `pickOrUndefined` restores all three states); received-date changes refresh every (unsaved) line's preview (verified live: backdate flipped auto #1002→#1003); part swap resets to auto (§5.2). Hub: Lines table Quote column shows the STORED link (ruling 6 — no re-fetch on date edits) + on-demand re-pick against the CURRENT received date that PATCHes only on explicit selection (untouched/closed = no request = updateLine's absent-key keep; current link excluded from options; "No quote" only while linked); add-rider gets the same picker (addLine is a create path, judged at the order's stored received date). Part page: `ActiveQuotesSection` per spec §4.2, served by the eligible route's absent-date=today default (no new read; orders.view gate NAMED when missing). Invoice-grid check: wiring coherent (frozen `sourceQuoteNumber` via `sourceLabel`); live run gated on fully-shipped, lands in Task 11's flow. Gates green (129 files / 2094 tests unchanged; tsc/eslint/build clean; E2E **18/18** on the third attempt — attempt 1 never booted (the smoke session's preview dev server held Next's single-instance lock), attempt 2 was 16-green when the session turn died under it (SIGTERM teardown ran; unverified, counts for nothing), attempt 3 watched to its exit sentinel; fixtures cleaned after every attempt incl. this task's smoke rows deleted via the app's own delete paths). 4 documented deviations (report) — notable: the add-rider form got the full picker (spec §5.2 names addLine as an entry path) |
| 10. Quote PDF + print + `User.title` | ✅ DONE (fix round 1: report amendments) | subagent (task-10) | ✅ Approved, Spec ✅ (task-reviewer round 2 — all three round-1 Importants closed: the process-defect acknowledgment self-disclosed a SECOND pre-written claim (the fixture-cleanliness sentence) the reviewer had not found; the RED section attests only what was watched (the cert-title 1-failed/1-passed split cited as the house example of a diagnostic service RED) and declines the rest on the record; the docs-commit question was a stale package-base read. 3 Minors carried → triage: no individually-watched REDs for the cent cases/reprint/route perms (values independently re-verified by the reviewer), deviation-5 range wording, and the round-1 self-flagged trio: ops[i] index coupling without a length assert, TitleCell ungated (page-level pre-existing), float qty×eachWeight) | The eighth document type end-to-end (4 commits: `16f817c` server, `cd2c598` User.title + cert title, `e45e346` UI, `39cf88a` a TS2698 builder fix that had missed its commit — caught by the controller's tree check, gates rerun after it). `pdf/quote.ts` pure builder to the sample (spec §6 transcription; footer PAGE CALLBACK sanctioned by §6's code-not-template carve-out, so no JSON-round-trip test — callback output content-pinned instead); `readQuotePdfData` collector + `printQuote` (claim → assertPrintable → render → storeDocument kind QUOTE, one Serializable tx — the printInvoice shape); **indicative amounts through the real engine** (`priceOrder` fed one synthetic line from quotedQty × each-weight, amounts read back off the OPERATION lines in order; null for unlimited/no-qty lines and per-row for LB-without-weight); `listDocumentsForQuote` + `GET /api/quotes/[id]/documents` at exactly Task 8's wired path; `POST /api/quotes/[id]/print` gated `quotes.view` (traveler/cert/invoice precedent, cited), filename `quote-N.pdf` the cert-route way. Deleted quote = VOIDED_PRINT 400 with stored prints downloadable forever (the discarded-invoice mirror); CLOSED/expired quotes print fine (tested). `User.title`: audited update + listUsers + PUT body + `SNAPSHOT_SELECT.user` (without which the diff showed nothing) + admin page Title column (PATCH-on-blur); cert signature line closed collector-side (`CertSignerRow.title` — the builder had rendered it since Phase 4). QuoteDetail Print button live (InvoiceDetail handler shape, docs list refresh counter). Tests +28 (quote-pdf 25 new; users +1; cert-pdf +2 EXTENDED, none rewritten). Gates green (130 files / 2122 tests; tsc/eslint/build clean; E2E 18/18 — CONTROLLER-verified after the implementer's watched runs were killed three times, with the surviving Task 9 `T9SMK` smoke fixtures purged in the same pass). 9 layout deviations documented for the demo (report) — notable: page count bottom-right via the callback, 5A vocabulary over VS labels, setup-on-top makes the sample's $100 row $102, 4-decimal unit prices |
| 11. E2E flow + docs + final gates | ✅ DONE | subagent (task-11) | (none — this task assembles the whole-branch review's inputs; its own reviewer IS that review) | `quotes` lands as the 19th and last flow (`e2e/flows/quotes.mjs` — the plan's `tests/e2e/quotes.spec.ts` name deviates to the harness's real `run.mjs`+`flows/*.mjs` convention, every prior phase's precedent): ending statement created+defaulted on the reference page (prior dev-DB default snapshot-and-restored — the priorBillingConfig precedent), quote with part-linked priced line incl. a break + free-text line with eachWeight, entry defaults asserted (number/dates/default statement), print→Documents, entry-page "Quote #N" auto-link preview BEFORE save, hub stored link, ship complete, DRAFT invoice with ONE op row sourced "Quote #N" at the quote's own 75.00 (setup+max(unit,min); the fixture part has NO PartPrice, so needs-price=0 structurally pins tier-1), close-with-reason warn-list naming the not-fully-invoiced order, second quote backdated into BOTH §5.4 sections. Invoice deliberately never finalizes (close warning needs it unfinalized; runs after close-month-end's still-closed month — draft creation isn't period-guarded; no finalizedAt to leak into close scope). Two flow-side wrong expectations caught live, zero product defects: the app's date-only "today" is the UTC calendar day, and the 5A Plus/Or minimum reading. Docs: HANDOFF §4 build-complete state + assembled owner-ratification queue (8 items); CLAUDE.md gains the §5.14 SSI-pairing STANDING INVARIANT sentence (row-locks paragraph) and quote-links.ts in the leaf enumeration; original spec §15 Phase 6 amendment table VERIFIED in place (nothing duplicated). Gates green, all watched (2026-08-11): **130 files / 2122 tests; tsc/eslint/build clean; E2E 19/19** with cleanup ok and the dev DB re-verified clean (one pre-existing note: 4 orphaned entity-"quote" audit rows, actor "Administrator", from the Task 10 controller smoke purge — not this flow's, left untouched, flagged for triage) |
| 12. Fix wave: ruling 7's overlap-save warning | ✅ DONE | subagent (task-12) | (owner-directed in-phase build of the whole-branch review's F1 — the review IS its mandate; report committed for the next review pass) | Commit `7d5a64d`. The §5.7 `shipmentWarnings` shape: `createQuote`/`updateQuote`/`attachPart` return `QuoteMutationResult = QuoteDetail & { warnings: string[] }`, computed in-transaction after the write — one warning per (part, other-quote) where another live OPEN quote holds a live line for the same part over an INCLUSIVE-boundary window overlap; free-text lines never warn; warns never block. Customer scoping verified implicit via partId (required `Part.customerId` + both `QuoteLine.partId` writers refuse another customer's part). UI: ShipmentDetail's amber banner on quote save/attach (state written ONLY by mutation handlers — §5.13: no reload can clear it; navigation dismisses), NewShipment's stop-on-warnings panel on create (zero warnings navigates as before). TDD RED-first: 11 new vitest cases + 3 route ride-through assertions watched RED (`warnings` undefined on successful responses), GREEN from the service change alone, message content pinned verbatim in two tests. One documented deviation: warnings ride EVERY update response, not only `lines` payloads (a date-only PATCH can create/dissolve the overlap — the brief's own shrink case; §5.7 full-surface precedent). Gates green watched (130 files / **2133 tests**, +11; tsc/eslint/build clean; **E2E 19/19** exit 0, cleanup ok, dev DB re-verified clean). Live scripted-Playwright smoke of both banners against the dev server, fixtures purged incl. their audit rows, counter restored (the 4 pre-existing orphaned audit rows left untouched) |

## Ready for whole-branch review (2026-08-11)

All 11 tasks DONE and task-approved; final gates green and watched (130 files / 2122 tests;
tsc/eslint/build clean; E2E 19/19, dev-DB fixtures verified clean). The review's inputs, assembled:

**Owner-ratification queue** (HANDOFF §4 carries the same list; task-11-report.md has the detail):
(1) inactive-part acceptance asymmetry on quote lines (T3); (2) CLOSED-quote-blocks-delete (T7 —
reviewer ruled it right, ratify); (3) dormant-column audit churn on first save after attach-part
(T4/T8); (4) grid-all-sources vs PDF-QUOTE-only source labels (T6 → demo); (5) part-page
Active-quotes gate area — orders.view vs parts.view/quotes.view (T9); (6) quotedBy picker options
need manage_users (T8/T10); (7) QuoteLine.eachWeight at the Part's real Decimal(10,4), spec
corrected in place (T1); (8) the quote PDF's 9 documented layout deviations (T10 → demo).

**Deferred-minor triage inputs, by task** (each task's row above carries its own full wording):
- T2: soft-deleted-row promote 200s silently (inherited generic-service hole); the latent
  Serializable-snapshot precondition comment if a ref kind ever gains an FK column; §5.16 polish
  on the inactive-row Default checkbox; RED-window note.
- T4: same-customerId-only payload mints a no-op audit entry; warn-list SHIPPED/REOPENED literal
  coverage rows.
- T5: judgeQuoteLine's walk → naming-only with the query as verdict; the SSI test's 200ms sleep
  can false-RED under load (never false-GREEN); create-vs-update audit shape asymmetry.
- T6: zero-net LEAD-line asymmetry (500 vs skip — throw is the safe direction); sourceQuoteNumber
  forgeable on the manual-lines save (cheap .refine); recalculate link-removed/link-added
  coverage wishes.
- T7: NO dangerous-direction test for the two new delete guards (the customerId-immutability
  dependency is load-bearing and untested in that direction — ticket-worthy); RED narration
  explicit only for the liveWhere exercise.
- T9: LineQuoteRepick lacks the sibling's useLatest out-of-order guard; pure-logic vitest wish on
  pickOrUndefined.
- T10: no individually-watched REDs for the cent cases/reprint/route perms (values independently
  re-verified by the reviewer); ops[i] index coupling without a length assert; TitleCell ungated
  (page-level pre-existing); float qty×eachWeight.
- T11: 4 pre-existing orphaned entity-"quote" AuditLog rows in the dev DB (actor "Administrator",
  the Task 10 controller smoke purge removed the quote rows but not their audit rows) — dev-DB
  hygiene note, no code change on the branch.

Review package: base = the branch point off `main`, head = the final docs commit; the per-task
`review-*.diff` packages remain regenerable from the SHAs in each row.

## Whole-branch review (2026-08-11, strongest model, full 16k-line diff read)

**Verdict: READY FOR PR — EMPTY mandatory fix wave.** No correctness/concurrency/data-integrity
defect survived verification anywhere on the branch (the three-of-four-phases blocking-defect
precedent does not repeat). Every hunt-list seam verified clean: all cross-transaction reads under
the right claim/isolation, the frozen/live boundary end-to-end (incl. REOPENED re-invoice and
credit copy), print-vs-delete both claiming the row, the finalize gap closed in vitest, GL/5C
untouched by quote-priced lines (readiness has no priceSource branch; finalize refuses needs-price
so an empty agreement can never reach export), registries/sweeps complete, free-text-line linkage
verified impossible.

**Headline finding F1 (the cross-task seam per-task review cannot see): spec §3 ruling 7's
overlap-save warning was never built** — the clause ("saving a quote that overlaps an existing open
quote for the same part warns but doesn't block") never made it from the spec into any task brief,
so eleven approved tasks each satisfied a brief that lacked it. Not a defect (issue-grade by the
triage rule) but a §3 contract clause the branch ships without → owner-ratification queue item 9;
owner decides in-phase build (small additive warn) vs issue.

**Issues to file (none fix-wave):** F1 overlap warn; F2 dangerous-direction tests for the
deletePart/deleteCustomer↔quote-writer SSI pairings (holes verified NOT live today; customerId
immutability is the load-bearing dependency); F3 zero-net lead-line corrupt-link asymmetry;
F4 indicativeAmounts ops[i] length assert; F5 sourceQuoteNumber .refine on the manual-lines save;
F6 soft-deleted-row promote 200s (inherited generic-service hole); F7 minors (no-op audit entry on
same-customerId PATCH, SSI-test sleep flake window, LineQuoteRepick useLatest, TitleCell page-level
gating note, float indicative display math, dev-DB orphaned audit rows).

**Controller verification the same day:** E2E independently rerun — all 19 flows passed, exit 0,
dev DB verified clean after (0 quotes / 0 orders / no stray ending statement).

## Fix wave (2026-08-11 owner ruling → built 2026-08-12)

The owner ruled F1 **built in-phase**: Task 12 (brief `task-12-brief.md`, report
`task-12-report.md`, row above) landed ruling 7's overlap-save warning as commit `7d5a64d` —
warnings on create/update/attach-part riding the responses per the §5.7 precedent, the amber
non-blocking banners in the quote UI, TDD RED-first with message-content assertions. F2–F7
remain issues to file as the review triaged them; ratification-queue item 9 is thereby resolved

## Task 12 review verdict (2026-08-11)

✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important). The reviewer verified the
inclusive-boundary predicate, warns-never-block on every path, the customer-scoping-via-partId
claim in both writers, §5.13 banner discipline (exactly two write sites), and the RED narration's
failure shapes against the assertions as written. 3 Minors → issues/demo: the advisory reads widen
the Serializable SSI footprint (transient 409 exposure class that already existed — the one sense
in which the computation can fail a mutation, now on the record), the stale banner on a closed
quote (demo), the create stop-panel's missing dismiss affordance (demo/polish). Controller
spot-checks the same day: dev DB clean (0/0/0), quote_number_next restored to 1015, queue-item-9
mapping confirmed.
(the remaining 8 owner-ratification items stand).

## PR + issues (2026-08-12)

**PR #94** opened (`phase-6-quoting` → `main`, attribution in the body). Deferred findings filed:
**#95** (F2 SSI-pairing tripwires), **#96** (F3 zero-net lead asymmetry), **#97** (F4 ops[i] length
assert), **#98** (F5 sourceQuoteNumber refine), **#99** (F6 dead-row promote 200), **#100** (F7 +
task-minor bundle incl. the Task 12 SSI-footprint note and the two demo-polish items). The 8-item
owner-ratification queue stands in HANDOFF §4; queue item 9 (ruling 7 warn) was resolved on-branch
by Task 12.
