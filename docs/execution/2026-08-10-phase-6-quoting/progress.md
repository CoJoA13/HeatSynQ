# Phase 6 — Quoting — Execution Ledger

**Branch:** `phase-6-quoting` · **Spec:** `docs/superpowers/specs/2026-08-10-phase-6-quoting-design.md` (approved 2026-08-10) · **Plan:** `docs/superpowers/plans/2026-08-10-phase-6-quoting.md` (11 tasks)

Committed on the first task per the standing rule (the `.superpowers/sdd/` gitignore-clobber lesson —
git ignore rules only bite untracked paths, so committing early closes the exposure window).

## Design session (2026-08-10)

One-question-at-a-time owner brainstorm; fourteen rulings recorded in the spec's §3. The owner
supplied `docs/samples/Quote_Sample_Form.jpeg` mid-session (VS stock quote form — the PDF build
target) and approved the spec including the seven flagged precedent-based calls (notes pair;
close/reopen under `quotes.edit`; delete-with-reason + §5.14 block; immutable `customerId`;
empty linked quote = needs-price, never part-price fallback; contact delete not blocked;
no attachments this phase).

Code-hook verification done before the brainstorm: `quotes` area exists (`permission-constants.ts`),
`quote_number_next` already seeded in settings (consumed by nothing — Phase 6 only wires it),
NO Quote model / order column / auto-link exists anywhere (the kickoff's "order entry already
auto-links" was the spec's rule, not built code), tier-1's insertion point is the per-line
`PriceRowInput[]` assembly in `invoices.ts` (`listPartPrices` at ~line 401), `PriceSource` and
`DocumentKind` are both DB enums (⇒ two `ADD VALUE`s in one earlier migration directory).

## Task ledger

| Task | Status | Implementer | Review | Notes |
|---|---|---|---|---|
| 1. Schema, migrations, registrations | ✅ DONE | subagent (task-01) | ✅ Approved, Spec ✅ (task-reviewer, round 1 — zero Critical/Important) | Data layer + 2 hand-written migrations on both DBs + all registrations; gates green (126 files / 1952 tests). Reviewer verified the restated CHECK arm-by-arm, the enum split, and that both sweeps were strengthened. Two spec-text deviations for owner ratification (surfaced in controller summary): eachWeight mirrored at Part's real (10,4) — spec corrected in place; QuoteLine.partId blocker deferred to Task 7 (registry is structurally reference-kind-only). endingStatement pulled forward as bare BlockerTarget — Task 2 absorbs. Minor: no-RED-narration note (schema-only task, sanctioned) |
| 2. `endingStatement` reference kind | ✅ DONE | subagent (task-02) | ✅ Approved, Spec ✅ (task-reviewer, round 1 — zero Critical/Important; 4 Minor deferred to whole-branch triage: soft-deleted-row promote 200s silently (inherited generic-service hole, same as terms), a latent Serializable-snapshot precondition worth one comment sentence if a ref kind ever gains an FK column, §5.16 polish on the inactive-row Default checkbox, RED-window note matching the close-periods precedent) | Eleventh kind wired through the generic machinery (constants, EXTRA_SCHEMAS, new "boolean" extra-field kind for the grid/export/paste, picklist by derivation); Task 1's three temporary shims absorbed. At-most-one-live-default enforced in the service under `pg_advisory_xact_lock(4300, 0)` (predicate invariant — the period-lock precedent), demotions audited; concurrency test RED-verified (lock removed from the promote branch → two live defaults survive, competing caller pinned Read Committed). Gates green (126 files / 1967 tests, +15; tsc/eslint/build clean; E2E 18/18 — reference screen changed, fixtures cleaned) |
| 3. Quote service: create/read/list/worklist | ✅ DONE (fix round 1 applied) | subagent (task-03) | ✅ Approved, Spec ✅ (task-reviewer round 2 — both Importants + 2 Minors verified closed; the reviewer confirmed the NOT{} substitution would go red by SQL three-valued-logic analysis. Carried to Task 4: SNAPSHOT_INCLUDE.quote first exercised there; carried to owner: the refuse-inactive-customer / accept-inactive-part asymmetry) | `quotes.ts` (createQuote/getQuote/listQuotes/quoteWorklist/exportQuotes) + `quote-constants.ts`; tests 5 → 43 in quotes.test.ts (fix round: inactive-customer refusal + inactive-part acceptance — asymmetry queued for owner ratification; `followUpDue: false` NULL-date complement RED-checked against a Prisma NOT{} swap; deleted/unknown explicit endingStatementId; expired:false at the =today boundary). Part-prices mirrored (shared decimalField scales; `LOT_WITH_BREAKS` now exported from part-prices.ts and imported, not re-declared). RED-verified: the one-live-line-per-part payload dup (service check removed → the create LANDS both lines; the (quoteLineId, stepCode) partial unique never sees it) and the concurrent create (naive unguarded counter read + Read Committed → both read 1000, loser is a 400 the test refuses; restored → distinct consecutive numbers, losers clean 409s). §5.4 boundaries pinned: followUpDate ≤ today, expiryDate < today, both OPEN+live, one quote in both sections. Gates green post-fix (126 files / 2005 tests; tsc/eslint/build clean; E2E n/a — no UI/route). 10 documented deviations (report §Deviations, deviation 4 corrected in the fix round) — notable: quotedQty×unlimited contradiction 400s, linked-part `active` NOT checked (brief says "live"), derived-filter complements avoid Prisma NOT over nullable followUpDate |
| 4. Quote service: update/close/delete + routes | ✅ DONE | subagent (task-04) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical; 1 Important FOLDED INTO TASK 5 as mandatory work: the §5.14 SSI pairing is recorded only quote-side while orders.ts still says "Serializable for uniformity" — Task 5 must add the load-bearing comment (naming BOTH halves: isolation AND the link writer reading the quote line in the same Serializable tx) plus the dangerous-direction test. 4 Minors: `linesPayloadFrom` omits eachWeight (→ Task 8 brief, silent-null hazard on free-text lines), same-customerId-only payload mints a no-op audit entry (→ triage), one-sentence comment on the spec-sanctioned link-onto-just-closed race (→ Task 5), warn-list SHIPPED/REOPENED literal coverage rows (→ triage). Reviewer independently traced the SSI cycle and the stamp-then-remint vs non-deferrable partial uniques — both sound) | Mutation half of `quotes.ts` (updateQuote/attachPart/closeQuote/reopenQuote/deleteQuote/quoteOrderBlockers, all under a `claimQuote` SELECT…FOR UPDATE) + the full `/api/quotes*` route surface minus print (T10) and eligible (T5). Isolation: update Serializable iff the payload assigns registered FKs (`assignsFk` — the `lines` arm also SSI-pairs the §5.14 OrderLine-predicate guard with the Serializable order save), attach/delete always Serializable, close/reopen claim-only. Array-replace is diff-and-write: kept ids update IN PLACE (`OrderLine.quoteLineId` stays valid — no delete-and-recreate precedent could give that), step-code/threshold re-keys stamp+re-mint (partial-unique swap hazard), stamps before creates. Close warns-and-lists open not-fully-invoiced linked orders (`finalizedInvoicesFor`, never `Order.status`); reopen clears close fields (audit before-snapshot keeps the story); SNAPSHOT_INCLUDE.quote first exercised with content-asserted diffs. RED-verified: close-vs-update race with the claim removed → the edit landed on the CLOSED quote. Gates green (127 files / 2040 tests, +35; tsc/eslint/build clean; E2E n/a — no UI). 9 documented deviations (report) |
| 5. Eligibility leaf + order auto-link | ✅ DONE | subagent (task-05) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important; both MANDATORY halves verified: the load-bearing comment names isolation + the in-tx read, the dangerous-direction test's commit order is barrier-forced so the abort lands deterministically on the save. 4 Minors → triage: judgeQuoteLine's walk should become naming-only with the query as verdict; the SSI test's 200ms sleep can false-RED under load (never false-GREEN); create-vs-update audit shape asymmetry (cosmetic, self-flagged); RED narration complete only for the SSI test) | `quote-links.ts` LEAF (eligibleQuoteLines/resolveAutoLink/judgeQuoteLine — the §5.2 rule ONCE, inclusive both ends, ruling-7 ordering; throws nothing, Prisma types + date lib only) + the three-way `quoteLineId` semantics in createOrder/addLine/updateLine (explicit id judged with line+reason named; explicit null; absent = auto-resolve on create/addLine, KEEP on updateLine — ruling 6) + `GET /api/quotes/eligible` (orders-area view, §5.15 reasoning) + per-line quoteLineId/quoteId/quoteNumber on OrderDetail. MANDATORY items shipped: createOrder's ⚠️ load-bearing-Serializable comment (both halves named + the spec-sanctioned close-race one-liner), the section comment carve-out, and the dangerous-direction test (gated REAL createOrder vs REAL updateQuote line-drop; RED-verified by pinning saveNewOrder to ReadCommitted — both committed, link landed on the soft-deleted line, post-mortem row captured; restored → SSI aborts the save 40001→409). SNAPSHOT_INCLUDE.order extended (lines pull quoteLine→quoteNumber; link edits read "1080 → 1085" in history). linkedOpenOrders left quote-side (sanctioned). Gates green (128 files / 2070 tests, +30; tsc/eslint/build clean; E2E 18/18, fixtures cleaned). 4 documented deviations (report) |
| 6. Tier-1 invoice substitution | ✅ DONE | subagent (task-06) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important. Reviewer RULED the no-source needs-price line spec-supported (tier 3 has no source; the link stays discoverable order-side). 4 Minors → triage: zero-net LEAD-line asymmetry 500s where non-lead skips (throw is the safe direction; report note corrected by the reviewer), sourceQuoteNumber forgeable on the manual-lines save within that surface's existing trust model (cheap .refine suggested), recalculate link-removed/link-added coverage wishes, grid-all-sources vs PDF-QUOTE-only routed to the demo) | Engine pass-through (`PriceRowInput.priceSource`/`sourceQuoteNumber`, OPERATION line emits what the row carries — no math change, existing suite green); both assembly sites branched (`buildPricingInput`'s per-line loop — shared by create AND recalculate — and createInvoiceInTx's lead-price `processNames` path): a linked line's rows come from the quote line's LIVE `QuotePrice`/breaks WHOLESALE (part rows not fetched; empty agreement → needs-price, RED-verified part rows absent), GL/ordering mirroring `listPartPrices` exactly, reads on the invoice tx's own client (#60 lesson; #60 itself untouched). `sourceQuoteNumber` frozen at line write and carried by credit copy, recalculate's manual-line re-create, `LINE_INPUT` round-trip; display reads the frozen column unconditionally — UI grid names every op line's source ("Quote #1006"), PDF annotates QUOTE lines only (5A sample fidelity). Dead/mismatched link = plain Error (corrupt state per §5.14 — never a silent part re-price), fabricated raw + tested. Gates green (129 files / 2084 tests, +14; tsc/eslint/build clean; E2E 18/18 — invoice grid + PDF display changed; fixtures cleaned). 5 documented deviations (report) |
| 7. Cross-entity §5.14 blocks | ✅ DONE | subagent (task-07) | ✅ Approved, Spec ✅ (task-reviewer round 1 — zero Critical/Important. Reviewer independently verified BOTH SSI pairings (deletePart↔quote-line writers all Serializable with in-tx reads; deleteCustomer↔createQuote sufficient because customerId is immutable) and ruled the CLOSED-quote-blocks call clearly right under the standing-agreement model (resolution path orders→quote→part/customer, never a deadlock) — still queued for owner ratification. 3 Minors → triage: no dangerous-direction test for the two new guards (the customerId-immutability dependency is load-bearing and untested in that direction — ticket-worthy), RED narration explicit only for the liveWhere exercise, quotedById examined post-review: follows the display-only model like closedById/cert signers — no block, consistent with every document's deleted-creator behavior) | Every quote-blockable delete path now refuses-and-names. `deletePart` + `deleteCustomer` gain in-tx quote-count guards (live = `deletedAt` only — CLOSED blocks, deleted never; counted per quote so refusal and panel agree) with `partQuoteBlockers`/`customerQuoteBlockers` naming each quote the registry way ("Quote · #1006", linked); both blocker routes + Excel exports serve the full union (order+quote / part+order+quote, workbook contents asserted); both detail pages' matchers extended so the existing BlockerPanel renders the new refusals. Step-code (QUOTE_VIA_PRICE) and endingStatement blocks verified behaviorally with zero new code — the Task 1 registry entries carry them; the from-the-grave case pinned at all three chain levels and RED-verified (quote-level liveness dropped from liveWhere → the dead-quote test caught it). Contact deletion pinned NOT blocked through the real `deleteContact` service (coverage was read-side only). Parts-side enforcement documented as blocker-list-IS-the-enforcement (no sweep analogue — registry is structurally reference-kind-only; comments in reference-links.ts + the sweep say a new Part-referencing FK is added by hand). Gates green (129 files / 2094 tests, +10; tsc/eslint/build clean; E2E 18/18 — both delete panels changed; fixtures cleaned). 4 documented deviations (report) — notable: CLOSED-quote-blocks queued for owner ratification |
| 8. `/quotes` UI | — | | | |
| 9. Order entry / hub / part page surfaces | — | | | |
| 10. Quote PDF + print + `User.title` | — | | | |
| 11. E2E flow + docs + final gates | — | | | |
