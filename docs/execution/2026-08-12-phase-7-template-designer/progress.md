# Phase 7 — Template Designer — Execution Ledger

**Branch:** `phase-7-template-designer` (off `main` at `c5c1f62`)
**Spec:** `docs/superpowers/specs/2026-08-12-phase-7-template-designer-design.md` (owner-approved 2026-08-12, incl. `pdf-lib`)
**Plan:** `docs/superpowers/plans/2026-08-12-phase-7-template-designer.md` (owner-approved 2026-08-12; 21 tasks; two-lens plan review incorporated)
**Process:** fresh subagent per task → task-reviewer (spec compliance + quality) → fix rounds until approved → whole-branch review on the strongest model → one fix wave → PR (attribution in the body). Round-6+ findings triage to issues unless correctness/concurrency/data-integrity.

**Gate rows in this ledger are written after watching the run end, from the run's own output — or they say PENDING.** (The Phase 6 Task 10 rule.)

## Baseline (2026-08-12, before Task 1, watched)

| Gate | Result | Timing |
|---|---|---|
| vitest | 2133/2133, 130 files | 219.8s |
| tsc --noEmit | clean | 2.1s |
| eslint src tests | clean | 10.0s |
| build | exit 0 | ~60s |
| E2E | not run (baseline; no change) | — |

Both DBs: 32 migrations, `migrate status` clean. Docker active, `erp-db-1` healthy.

## Task ledger

| # | Task | Implementer | Review | State |
|---|---|---|---|---|
| 1 | Contract machinery + order-side contracts | subagent 2026-08-12 (`6142d33`, `0cae14f`; report filed; gates watched: vitest 2195/2195 in 213.4s, tsc 1.6s, eslint 9.0s, build exit 0 in 15.5s; E2E n/a — no UI/flow) | Spec ✅ / **Approved** (round 1; every binding value verified verbatim against the builders; 4 minors, none blocking — carried below) | **APPROVED** |
| 2 | Billing-side contracts | subagent 2026-08-12 (`2b3a37a` the carried section-hide lock fix, `eca1c9a` the four contracts + registry; report filed with RED evidence; gates watched: vitest 2226/2226 in 206.7s, tsc 1.9s, eslint 9.2s, build exit 0 in 16.9s; E2E n/a — no UI/flow) | Spec ✅ / **Approved** (round 1; zero drift in any pinned value; 3 minors — 2 routed to Task 3 pre-steps, 1 to Task 14, carried below) | **APPROVED** |
| 3 | Schema, migrations, seeds, registrations | subagent 2026-08-12 (`bc887a4` the two pre-steps [MMMM D, YYYY + PRICE_SOURCE_LABELS.QUOTE], `8868112` schema + migrations 33/34 [`_document_templates`, `_seed_standard_templates`], `e4a960c` registrations + drift guard; report filed with RED evidence; gates watched: vitest 2240/2240, 132 files in 220.9s, tsc 1.7s, eslint 9.1s, build exit 0 in 15.8s; `migrate status` clean on BOTH DBs at 34; E2E n/a — no UI/flow) | Spec ✅ / **Approved** (round 1; migration SQL verified column-by-column incl. implicit FK behaviors; cascade judged safe — StoredDocument's own SET NULL keeps archived paper from dangling; drift guard proven non-vacuous; 3 minors → Task 4 pre-steps, carried below) | **APPROVED** |
| 4 | Template service | subagent 2026-08-13 (`6924a67` the three pre-steps [jsonb_set path pins, shared seed-id helpers, scoped 1-30 ban], `bf92e6d` service + image-sniff extraction, `4c8f0e0` routes, `ccefcf9` claimTemplate exported for Task 5; report filed with RED evidence incl. all four concurrency guards claim-removed/tx-split; gates watched on final HEAD: vitest 2298/2298, 134 files in 250.4s, tsc 1.8s, eslint 10.0s, build exit 0 in 17.0s with all 8 template routes in the manifest; E2E n/a — routes only, no UI/flow) | Spec ✅ / **Approved** (round 1; setDefault's docType-wide claim judged claimOrdersInOrder-done-right with deadlock analysis; updatedAt precondition atomic; immutability pinned twice; all 5 deviations accepted; 4 minors carried below) | **APPROVED** |
| 5 | Assignment + resolution | subagent 2026-08-13 (`a9c4fb1` service [claim-first assign via the exported `claimTemplate`, reason-free clear, §5.2 walk-to-root resolution with both-deletedAts filter + cycle bound + never-null] + deleteCustomer cascade + the two routes [customers.edit + edit_templates on mutations; `requireUser`-only /api/templates/names per §5.15, picklists sweep shape] + 32 tests; report filed with RED evidence incl. the delete-vs-assign race claim-removed in both directions; gates watched on final HEAD: vitest 2330/2330, 135 files in 243.0s, tsc 1.9s, eslint 10.0s, build exit 0 in 16.5s with both routes in the manifest; E2E n/a — routes only, no UI/flow) | Spec ✅ / **Approved** (round 1; walk/cycle-bound/backfill/never-null all verified in code; race evidence judged genuine; clear-without-claim reasoning held under adversarial reading; 4 minors — 3 → Task 6 pre-steps, 1 → Task 20 brief note) | **APPROVED** |
| 6 | Render runtime | subagent 2026-08-13 (`fdd3b3c` the three pre-steps [replace-guard `deletedAt: null` RED-verified with a parked-holder race, GET 403 case, return-shape select-down], `4f11652` pageFooterSpec/continuationHeaderSpec + unregistered-font belt + jpegDataUri, `9a29ab6` renderSheetGroups [pdf-lib ^1.17.1, only import site render.ts, `useObjectStreams: false`, per-group restart proven page-by-page], `4a29afd` the 4-family font map [Liberation Sans/Serif from Fedora's packages, Roboto Mono from googlefonts/RobotoMono at google/fonts' pinned commit — the aggregate repo carries only variable TTFs; OFL/license files + PROVENANCE.md with per-file sha256; Roboto keeps the vfs decode under its unchanged key] + `outputFileTracingIncludes` with all 12 .ttf PHYSICALLY verified in `.next/standalone`; report filed with RED evidence per cycle; tests decode RENDERED bytes via ToUnicode CMaps with Tf tracking; gates watched on final HEAD: vitest 2352/2352, 136 files in 239.4s, tsc 1.8s, eslint 9.5s, build exit 0 in 16.4s, **E2E all 19 flows passed EXIT 0** — run 1 hit a `quotes` fill-timing flake at 18/19 [no quote UI touched on this branch; stable row keys; green at Phase 6 close], full re-run passed 19/19 watched via sentinel; dev-DB fixture hygiene verified 0 rows after both runs) | Spec ✅ / **Approved** (round 1; pdf-lib confinement grep-verified; all 15 sha256 recomputed and matching; Docker cwd path traced clean via the Dockerfile; the flake judgment independently CONCURRED with mechanism analysis — on record below for whole-branch; Roboto Mono OFL deviation accepted as spec-aligned; 2 cosmetic minors carried below) | **APPROVED** |
| 7 | Traveler conversion + stamp plumbing | subagent 2026-08-13 (`a029266` decoder lifted to tests/helpers with the endstream guard, `b6144f9` storeDocument's optional templateVersionId, `3c5cf30` buildTravelerDefinition config-consumer + §5.6 builder belt + ruling-4 processName slot, `23e80d2` printTraveler resolves on its claimed tx + stamps; report filed; unit gates watched on `23e80d2`: vitest 2383/2383, 137 files in 238.8s, tsc 1.9s, eslint 9.3s, build exit 0; **E2E 19/19 controller-watched on `5e7b4b3`** after the quotes stale-gate fix — see the report's E2E-story note and `quotes-e2e-fix-report.md`) + **the quotes E2E root-cause fix `5e7b4b3`** (debug agent: pre-existing Phase 6 defect — QuoteDetail load effect had no stale gate; StrictMode double-mount's late GET wiped in-flight drafts; proven 2/2→0/2; supersedes Task 6's compile-pause hypothesis) | Spec ✅ / **Approved** (round 1, BOTH diffs; golden gate met "the strong way" — zero golden-file edits, contract constants byte-matched; QuoteDetail swept for other ungated adopt paths — none; 2 minors carried below) | **APPROVED** |
| 8 | Traveler sheet groups (#36, #43) | **three subagents 2026-08-13 — the first two were terminated mid-task by an infrastructure error** (not by their work); the third inherited, verified and closed out. `efac8c2` (agent 1: `completeSections`, the §5.6 belt's omission half — a client-safe leading-run merge exported from the contracts module for Tasks 9–14), `50e09dc` (agent 1: `overflowTopMargin` — the two-pass render reserves continuation-header room ONLY when a sheet overflows, so page one of every existing print is untouched), `eb718ac` (agent 3: the ToUnicode decoder dropped LIGATURE destinations — `fl` → `<0066 006c>` — shifting every later glyph and decoding whole pages as a substitution cipher; latent since Task 6, exposed by this task's "Overflow Co" fixture; + `paintedImageCounts`), `6643621` (#36: one definition per load via `renderSheetGroups`, each with ITS load's identity band; shared `prepareSheets` so singular/plural per-sheet content cannot drift; `pageFooter` restarts per group; default margins byte-identical), `8538cab` (#43: `MAX_TRAVELER_LOADS_PER_PRINT = 100`, enforced where "every load" is decided so print AND preview refuse identically, before any render — the claim never spans an unbounded render). Agent 2's WIP was inherited UNCOMMITTED and verified rather than adopted: tsc clean as it claimed, but the targeted run was 193/194 — the one failure being the decoder bug above. Report filed with RED evidence for both fixes (decoder: the garbled-page failure + a reverted-`parseCmap` run; #43: the four boundary tests re-run with the bound raised to 100_000 → exactly the two refusal tests fail, the two controls pass). Gates watched on final HEAD `8538cab`: vitest **2411/2411, 137 files** in 243.4s, tsc clean, eslint clean, build exit 0 in 15.9s, **E2E 19/19 PASS exit 0** (detached with a sentinel from the start; ~4.5 min); dev-DB fixtures verified 0 rows across all four prefixes after the run | Spec ✅ / **Approved** (round 1; false-pass audit CLEAN — no Task 6/7 assertion needs re-examination; both deviations judged improvements, the #43 placement better than the brief; decoder ligature fix confirmed root-cause-correct; takeover reported honestly; 3 trivial minors noted below) | **APPROVED** |
| 9 | Ticket + MOS conversion + tear-off reflow | subagent 2026-08-13 (`3a3be12` buildShippingTicketDefinition becomes the ONE config-consumer for BOTH ticket contracts [docType names which contract resolves the backfilled config; views over `completeSections` — the §5.6 omission half; nothing on these contracts is locked, so the flag belt is pinned in the NEGATIVE direction; **the two-date-styles trap honored**: the single date knob maps to the header `shortDate` slot ONLY, the tear-off keeps `paddedDate` unconditionally — both assertions in one test plus a five-style sweep], `444acae` tear-off FLOW-BASED [unbreakable stack + 16pt gap replaces `absolutePosition {24,648}`; `textRunsWithY` position decoder added ADDITIVELY to tests/helpers/pdf.ts — drawnPages untouched; RED with the exact overlap signature, a part row at device y≈98 under the strip's top y≈128; HANDOFF §7 item 5.3 closes], `0f2f3a5` per-ticket sheet groups [one RenderableDefinition per order's ticket via shared `prepareTickets` — plural pinned stack-for-stack to the singular oracle; identity band ignores visibility flags, label override carries; `pageFooter` default OFF golden, on → per-group pageNofM + 44pt bottom], `83d18a7` print path [docType = the SHIPMENT'S order count read under the existing claims — the per-order ticket of a multi-order shipment resolves MOS_SHIPPER, tested as all three §5.2 cases with marker templates; `resolveTemplateForPrint` on the claimed tx; logo per the traveler pattern; stamp on every stored row; liability text from the resolved config's text block injected at the settings seam — the Setting no longer reaches ticket paper, settings.ts untouched until Task 14]; report filed with RED evidence per cycle; golden gate met the strong way — tests/shipping-ticket.test.ts ZERO edits, green, plus the Phase 4 snapshot suites; gates watched on final HEAD `83d18a7`: vitest **2449/2449, 138 files** in 252.2s, tsc 1.8s, eslint 10.0s, build exit 0, **E2E 19/19 PASS** [detached with a sentinel from the start, read from the run's own log]; dev-DB fixtures verified 0 rows across all four prefixes before AND after) | Spec ✅ / **Approved** (round 1; the date-trap provably honored — the knob's only consumer is the header slot; tear-off reflow proven by coordinate-decoded non-overlap; §5.2 resolution correct by construction under a verified Serializable snapshot; deviation 3 ruled sufficient; unit gates INDEPENDENTLY re-run by the controller: 2449/2449, tsc, eslint clean; 3 documentation minors — 2 fixed by controller, 1 → Task 10 pre-step) | **APPROVED** |
| 10 | BOL conversion | subagent 2026-08-13 (`e93a8c3` the pre-step [the ~:1890 docType-count comment names the real mechanism now — the Serializable snapshot fixed at the stub read, not "under the claims"; comment only], `9c1152a` buildBolDefinition becomes a config-consumer [the ticket lens on the BOL's single contract: views over `completeSections` — the §5.6 omission half — PLUS a text-block fallback to the contract's `defaultText` for the one config surface completeSections doesn't cover; **the ELEVEN UDSBL legal constants deleted from the builder** — they render from `config.textBlocks` (spec §8), the contract's verbatim transcriptions the only remaining copy, every one of the eleven pinned config-fed individually; **the date-style grep finding**: `bolDate` had exactly ONE call site (the ship-from line), so the single knob maps directly — no two-styles trap, all five styles tested; smallSize → the six 5.5pt clauses only (knob-maps-to-what-the-default-reproduces); header-center logo materializes a middle column in the sample's two-slot header; **the overflow finding**: the orders array is unbounded (min(1), no max) and ~80 orders = 2 pages (probed empirically BEFORE building), so the continuation band is NOT dead code — identity = bolNumber under the `bol_number` label, override carries, visibility ignored, pinned through rendered bytes; pageFooter default OFF golden, margins [24,24,24,24] asserted], `c40872f` print path [`resolveTemplateForPrint(tx, "BOL", …)` inside the existing Serializable tx, NO docType ternary — count-independence pinned: single- AND two-order shipments resolve the same assigned marker template and stamp the same version id; logo per the ticket pattern; `storeDocument(…, resolved.versionId)`; **the lazy §3.19 allocation byte-identically untouched** — first-print-allocates/reprint-reuses regression pinned WITH stamps on both rows]; report filed with RED evidence per cycle [builder: 30 failed/7 vacuous-passed of 37; print path: all 6 red]; golden gate met the strong way — tests/bol.test.ts ZERO edits, 16/16 green; gates watched on final HEAD `c40872f`: vitest **2486/2486, 139 files** in 261.7s (+37), tsc 1.9s, eslint 11.2s, build exit 0, **E2E 19/19 PASS** [detached from the start with the PER-TASK sentinel `e2e-task10.done`, result read from the run's own log: full results block + "All 19 flows passed" + "cleanup ok" + EXIT:0]; dev-DB fixtures 0/0/0/0 before AND after; one deviation of note: the UDSBL text binds to `config.textBlocks` directly, NOT Task 9's data-seam suggestion — the brief's explicit instruction governs, and the report's decision 1 explains why the ticket's one-source-per-fact analysis doesn't transfer) | Spec ✅ / **Approved** (round 1; golden chain builder→contract→seed verified byte-for-byte across all three links; deviation brief-mandated and correctly argued; both investigate-first findings verified not assumed; 2 polish notes only) | **APPROVED** |
| 11 | Cert conversion | successor subagent 2026-08-13 (inherited `238cfbd` render `above` — Task-6 infra, built on: a per-page company strip rides the pageNofM knob's static `above` slot instead of competing for pdfmake's one `footer`; `d307194` buildCertDefinition becomes a config-consumer [the BOL lens on the cert's nine sections: views over `completeSections` — the §5.6 omission half; nothing locked, so the flag belt pinned NEGATIVE; **cert_statement binds at the DATA SEAM not config.textBlocks** — certs.ts already reads the Setting into CertPdfData.statement, so it is caller data like the ticket's liability text, NOT a builder literal like the BOL's constants: the builder reads input.statement verbatim, printCert injects the config block; **the date finding**: paddedDate was the SOLE date renderer with two call sites (Date + Entry Date) BOTH MM/DD/YYYY, so ONE style across two slots, the single knob maps to both — all five styles tested; the THREE untouchables preserved and pinned — §3.21 type-level exclusion intact (no field/binding added, CertPdfData still carries no min/max/pass-fail), the signature block byte-identical under default config pinned BOTH via a whole-node deepEqual AND rendered bytes (image paints [1] / typed-name fallback [0]), multi-part certs ONE sheet group with the ruling-27 frozen-identity grouping UNCHANGED (part_heading only gates heading visibility); **the overflow finding**: readings max(500)/parts unbounded/serials max(10000) each overflow (100 readings→2pp, 10 parts→2pp, 50 serials→2pp — probed empirically BEFORE building), so the continuation band is NOT dead code — identity = Order No. + orderLabel + (continued), override carries, visibility ignored; pageFooter default OFF golden, bottom margin 44 unconditional since the cert always carried a strip], `efb8fe8` print path [`resolveTemplateForPrint(tx, "CERT", owner.customerId)` inside the existing Serializable tx, NO docType count split — a cert is one per scope instance, registry has no MOS_CERT; cert_statement replaced at the readCertPdfData seam by resolved.config.textBlocks.cert_statement — the Setting no longer reaches paper, settings.ts untouched until Task 14; logo per the BOL pattern; storeDocument(…, resolved.versionId); **printedAt first-print + signer read byte-identically untouched**, regression pinned with stamps on both prints]; report filed with RED evidence per cycle [builder: 30 failed/12 golden-holding-passed of 42; print path: all 6 red]; golden gate met the strong way — tests/cert-pdf.test.ts ZERO edits, 36/36 green, plus the Phase 4 cert suites (cert-results/certs/cert-routes/cert-list/cert-resolution/certs-schema) + documents/template-seed/template-contracts, 276 tests across ten neighbor files, all unedited; gates watched on final HEAD `efb8fe8`: vitest **2530/2530, 140 files** in 297.6s (+44), tsc 5.1s, eslint 12.1s, build exit 0, **E2E 19/19 PASS** [detached from the start with the PER-TASK sentinel `e2e-task11.done`, result read from the run's own log: 19 PASS lines + "All 19 flows passed" + "cleanup ok" + EXIT:0]; dev-DB fixtures 0/0/0/0 before AND after; build deferred until E2E finished — both share `.next`) | Spec ✅ / **Approved** (round 1; all 3 untouchables pinned structurally AND through rendered bytes; cert_statement data-seam binding proven both directions; render `above` extension judged additive/safe; 3 cosmetic minors + 1 whole-branch note carried below) | **APPROVED** |
| 12 | Invoice/credit conversion (+#98) | subagent 2026-08-13 (`9f51e99` create-time processName snapshot [`Part.processName` non-blank else the priced-op comma-join, CREATE-TIME in `createInvoiceInTx`; `ORDER_LINE_SELECT`+`LeadPart` gain the field; recalc explicitly untouches it, credits copy the source] + #98's `sourceQuoteNumber`-requires-QUOTE `.refine` on `LINE_INPUT` [ZodError→400, both directions tested, echo-back intact], `9cbf8a7` **buildInvoiceDefinition becomes a config-consumer over FROZEN data** [the BOL/cert lens on the invoice's nine sections: views over `completeSections` — the §5.6 omission half; nothing locked, flag belt pinned NEGATIVE; **the four data columns are a GRID owned by `column_header`** — every body block aligns to those widths, a hidden column frees budget exactly as `assertWidthBudgets` models it, `parts` fields gate the per-row VALUE within the grid; title/company_name value-only DATA; the identity/order-strip label glue is builder-side (contract labels carry no trailing spaces); **the negativeStyle knob is the invoice's first real declarer** — `makeMoney` maps SIGN_AFTER_SYMBOL default "$-937.44"/LEADING_MINUS/PARENTHESES, all three tested on a credit fixture AND the real path; the date knob has no style trap (`longDate` sole renderer, one call site, five styles tested); priceDecimals/thousandsSeparator map to the one money/weight/qty surface; footer is a page-margin strip, bottom margin 44 unconditional; **the overflow finding**: order lines unbounded → 15 parts→2pp/40→3pp (probed empirically BEFORE building), so the continuation band is NOT dead code — identity = Invoice No.+documentNumber+(continued), override carries, visibility ignored], `827ab60` print path [`resolveTemplateForPrint(tx, "INVOICE", invoice.customerId)` inside the existing Serializable tx — BOTH an invoice and a credit resolve INVOICE (§4.1 one contract covers credits), pinned; logo per the BOL/cert pattern; `storeDocument(…, resolved.versionId)`; **`claimInvoiceForPrint` + print-vs-discard serialization byte-identically untouched**]; report filed with RED evidence per cycle [#98+processName: 5 failed/3 passed of 8; builder: 24 failed/17 golden-holding-passed of 41; print path: all 7 red]; golden gate met the strong way — **tests/invoice-pdf.test.ts ZERO edits, 17/17 green**, plus the 5A invoices/invoice-routes/invoice-guards suites + template-contracts/template-seed/template-assignments, all unedited; gates watched on final HEAD `827ab60`: PENDING) | — | **IMPLEMENTED — gates+review pending** |
| 13 | Statement conversion (+#87) | — | — | — |
| 14 | Quote conversion (+#97, settings retirement) | — | — | — |
| 15 | Part.processName UI | — | — | — |
| 16 | Templates admin + nav | — | — | — |
| 17 | Editor panels + logo | — | — | — |
| 18 | Editor save/conflict UX | — | — | — |
| 19 | Preview | — | — | — |
| 20 | Customer-page assignment picker | — | — | — |
| 21 | Restyle E2E flow, docs, final gates | — | — | — |

## Carried minors (from per-task reviews; each routed to a named later task)

- **From Task 1 review → Task 2:** latent lock-bypass in the generic machinery — `assertLocksHonored` does not refuse hiding a *hideable* section that contains a non-removable field (Task 1's contracts compensate by pinning those sections non-hideable, but nothing enforces the pairing for later contracts). Task 2 fixes the semantics (a section-hide counts as hiding its fields for lock purposes) + tests it. **DONE in Task 2 (`2b3a37a`, RED-verified both directions).**
- **From Task 1 review → Task 9:** the shipping ticket prints TWO date styles (header `shortDate` M/D/YYYY vs tear-off `paddedDate` MM/DD/YYYY) against the contract's ONE date knob — Task 9's mapping must not let the single knob change the tear-off at the golden-compat gate (map the knob to the header; the tear-off keeps its own style, or gets its own knob).
- **From Task 1 review → Task 17 (editor):** `lockedElements` returns a flat `{key, reason}` list mixing section and field keys — tighten the namespace before rendering padlocks.
- **From Task 1 review → all future briefs:** implementer reports must include RED-run evidence (a failing-test output snippet), not just the claim.
- **From Task 2 review → Task 3 (pre-steps, BEFORE the seed literals freeze):** (a) the `"MMM D, YYYY"` date token is bound to the FULL-month `longDate` ("July 29, 2026") on the invoice/statement — rename the token to `"MMMM D, YYYY"` while no config is stored anywhere, or Tasks 12/13 implementing it per convention ("Jul 29") silently break golden compat; (b) the invoice contract's `quote_source` defaultLabel duplicates `PRICE_SOURCE_LABELS.QUOTE` (`src/lib/invoice-constants.ts`) — reference the client-safe constant, the aging-labels anti-drift rule. **Both routed into Task 3's brief. DONE in Task 3 (`bc887a4`, before any seed literal froze; the rename RED-verified, the label pinned to the constant in the test).**
- **From Task 2 review → Task 14:** the quote prints TWO money precisions against ONE `priceDecimals` knob — `money()` 2dp on setup/minimum/indicative amounts vs `money4()` 4dp on unit/break prices. Task 14's mapping applies the knob to unit/break prices ONLY, or a sub-cent setup charge changes at the golden-compat gate (the ticket two-date-styles analog).
- **From Task 3 review → Task 4 (pre-steps):** (a) the drift guard counts the seed's `Setting` subqueries but never pins the `jsonb_set` target paths — one `expect(SQL).toContain("'{textBlocks,<key>}'")` per key closes the typo'd-path residual risk (an applied migration is frozen, so this is documentation-grade, but cheap); (b) the `templateId()` id-minting helper is duplicated between `tests/helpers/db.ts` and `template-seed.test.ts` — export it once from the helpers (Task 4's fixtures will reference the same seeded ids). (c — cosmetic, optional) scope the whole-file `not.toContain("1-30")` ban to the statement literal. **All three DONE in Task 4 (`6924a67`).**
- **From Task 4 review → Task 18 (editor save UX):** same-millisecond ABA window on the `updatedAt` precondition (ms-precision `@updatedAt`; two round trips inside one ms of the last touch pass the equality) — practically unreachable, but the editor can additionally compare a returned config hash if it ever matters.
- **From Task 4 review → Task 16 (templates admin UI):** (a) `getTemplate` is two autocommit reads — a publish committing between them can return history showing a DRAFT while `draft` is null; wrap in one `$transaction` if the editor cares (self-healing on refresh). (b) The blockers export returns an empty workbook for an unknown/deleted id (the surcharge-export shape) — the UI must link the export from the §5.14 refusal only, never standalone.
- **From Task 4 review → Task 21 (docs pass):** CLAUDE.md's sample handler shape is stale — `requireUser()` is no-arg/synchronous (`http.ts:18`), the sample still shows `mustCan(await requireUser(req), …)`. Fix the sample in the docs pass (pre-existing staleness, not Phase 7's change).
- **From Task 5 review → Task 6 (pre-steps):** (a) `assignTemplate`'s replace path updates by bare id after a live read — a concurrent claim-free `clearAssignment` between them can rewrite a DEAD row's `templateId` (audit-trail smudge only, no invariant breaks); guard the update with `deletedAt: null` in the where. (b) GET assignment-list has no 403 test (bare session without `customers.view`). (c) `assignTemplate`'s declared return type is narrower than the full row PUT serializes — align or `select` down.
- **From Task 5 review → Task 20 (customer-page picker brief):** the `requireUser`-only names projection carries no published flag, so the picker cannot render a never-published template disabled-with-tooltip (§5.16) — either widen the projection with a boolean, or let the assign-time named 400 surface and say so in the UI.
- **From Task 6 review → whoever lifts the PDF decoder into `tests/helpers/`** (the implementer flagged the lift when a second suite needs it — likely Task 8): guard `parseObjects` against a missing `endstream` (today `-1` feeds `subarray`/`indexOf` and produces confusing failures instead of a clear parse error). Also cosmetic: the spec-collision check uses `!== undefined`, so an explicit `footer: undefined` beside a spec slips it — no builder writes that shape.
- **For the whole-branch review record:** Task 6's run-1 E2E failure (quotes setup-charge `locator.fill` detachment, 18/19) was judged pre-existing by the implementer AND independently concurred by the reviewer with mechanism analysis (zero client-code changes in the task; a render regression would fail at print, not form fill; dev-server compile-pause remount is the plausible cause). Full re-run 19/19 with zero code changes.
- **From Task 7 review → Task 8 (pre-step):** the §5.6 builder belt covers flag-flips but NOT entry omission — a validator-bypassing config that *omits* the steps section entry (or a locked field entry) drops the locked element; the belt only forces entries that are present (the barcode alone is immune). Unreachable through stored configs (the backfill re-adds missing entries), but the belt's own rationale applies: merge the config's section/field lists with the contract's key list (leading-run merge) so omission cannot drop anything, and test the omission shape. **Adopt the merged pattern in every remaining conversion (Tasks 9–14 briefs).** **DONE in Task 8 (`efac8c2`): `completeSections(contract, sections)`, exported from `src/lib/template-contracts/types.ts` (client-safe), adopted by the traveler builder in the same commit — Tasks 9–14 copy the HELPER, two lines each, not the pattern. Eight tests (five contract-module, three builder-side omission shapes).**
- **From Task 7 review, resolved by controller:** the task report's dangling fixture-count placeholder now cross-references quotes-e2e-fix-report.md.
- **From Task 8 review (notes, no action owed):** (a) RED evidence for the two inherited commits died with agent 1 — recorded here as unrecoverable, the omission tests are structurally non-vacuous; (b) the two-pass render's function-refusal walks top-level keys only (all legitimate pdfmake function keys ARE top-level; a doc-comment tightening at most); (c) the decoder's sequential-bfrange arm increments the last UTF-16 code unit vs the spec's last byte — divergent only on ranges conforming CMaps avoid; comment-worthy in the test helper.
- **From Task 11 review → Task 12 (pre-steps, cosmetic):** (a) `cert-templates.test.ts:1419` opens the §3.21 test with a no-op relabel loop (comment describes work it never does) — drop or make it relabel; (b) the print-path `if (!owner) throw 404` in certs.ts is unreachable (assertPrintable throws first for a null owner) — it's TS-narrowing only, the 404 never emits; leave or comment. Neither blocks; fold into Task 12's cert-adjacent cleanup only if convenient.
- **From Task 11 review → WHOLE-BRANCH REVIEW (record):** once `cert_statement` (and the other three standing texts at Task 14) move to `config.textBlocks`, a production deployment that CUSTOMIZED the Setting must have that value carried into template content. Task 3's seed migration does this via COALESCE at deploy time (reads the live Setting), and the seeded default byte-matches the code default, so the intended path is safe — BUT the whole-branch review should confirm the Task-14 retirement doesn't strand a Setting customized in the window between Task 3's migration and Task 14's key removal (no production deployment sees that window, since both are in this one release; confirm).
