# Task 14 brief — Quote conversion (the LAST conversion) + #97 + settings retirement completes

**Branch:** `phase-7-template-designer` (Tasks 1–13 APPROVED; six documents converted; suite at 2624; E2E 19/19). This is the eighth and final document conversion — after it, all eight types are templated.
**Read first:** the spec §5.4/§5.6 + **§6.1 (the quote's footer callback is the ONE code-not-data exemption — this task RETIRES it: the "Page: N of M" becomes `pageFooterSpec { kind: "pageNofM", label: "Page:" }`, and the quote builder JOINS the purity round-trip tests it was exempt from)** + **§8 (settings retirement — the four standing-text keys leave `settings.ts` HERE)** + plan Task 14; **the ledger's Task 2 carry (BINDING): the quote prints TWO money precisions against ONE `priceDecimals` knob — `money()` 2dp on setup/minimum/indicative-amount vs `money4()` 4dp on unit/break prices; the knob maps to UNIT/BREAK PRICES ONLY, never the 2dp `money()` calls** (a sub-cent setup charge changing at the golden gate is the failure); **Task 13's report** (its Task 14 notes: the footer-callback→pageNofM mapping with label "Page:"; the stranded-Setting whole-branch check; the #87 leaf + config-consumer lens copy verbatim); `gh issue view 97`. Then `erp/src/server/pdf/quote.ts` in full (esp. the `footer` callback ~:344 and every `money`/`money4` call site), `readQuotePdfData`/`printQuote` + `indicativeAmounts` in `erp/src/server/quotes.ts`, the quote contract, `settings.ts` (the four keys), `settings-ui.ts` (`TEXTAREA_KEYS`), and the settings tests naming those keys.

## Deliverable

1. **`buildQuoteDefinition(data, config)`** — config-consumer (`completeSections`, §5.6 both halves): sections/fields/labels/widths/fonts/formats. **The two-money-precisions trap is the headline** — grep every date and money call site first; map `priceDecimals` to the `money4` (unit/break price) calls ONLY; the `money()` 2dp calls (setup, minimum, indicative extended amounts) stay 2dp unconditionally. Pin it with a test that moves `priceDecimals` and asserts the unit price changes while the setup/minimum/amount do NOT. `quote_intro_text`/`quote_liability_text` bind from config (data-seam or config-binding — investigate what `quotes.ts` does today, the cert/BOL fork; justify). The default is `pageFooter: true` for the quote (Task 2 set it — the quote is the one document that prints a page line today; golden compat means keeping it ON).
2. **Retire the footer callback (§6.1):** delete `buildQuoteDefinition`'s hand-written `footer` callback; the "Page: N of M" now comes from `pageFooterSpec { kind: "pageNofM", label: "Page:" }` carried on the definition (render.ts builds the callback — Task 6). **The quote builder joins the purity round-trip tests** (delete its documented exemption in `tests/quote-pdf.test.ts` — the exemption comment/skip goes away; the builder's output must now survive `JSON.parse(JSON.stringify(...))`). Golden: the printed "Page: N of M" is byte-identical to today (label "Page:", same styling — Task 6's pageNofM reproduces the quote's exact footer styling by construction; verify).
3. **#97** (`gh issue view 97`): `indicativeAmounts` (`quotes.ts`) asserts `ops.length === line.prices.length` before the index map — throw naming the mismatch if it ever breaks (a future engine change that filters/reorders operations turns `ops[i]` into misaligned money or a print crash). Test the guard.
4. **`printQuote`** — resolve `resolveTemplateForPrint(tx, "QUOTE", quote.customerId)` on the claimed tx; logo; stamp `templateVersionId`; `claimQuote` untouched.
5. **Settings retirement completes (§8):** the four standing-text keys (`cert_statement`, `shipper_liability_text`, `quote_intro_text`, `quote_liability_text`) leave `settings.ts` and `settings-ui.ts`'s `TEXTAREA_KEYS`; the settings tests naming them are updated. Add the third migration `<ts>_retire_standing_text_settings/` that **DELETEs the orphaned `Setting` rows** (their values were copied into the seeds at Task 3 — verify by re-reading Task 3's seed migration that all four were COALESCE-copied). Apply to BOTH databases; `prisma generate`. **The stranded-Setting whole-branch check (from Task 11's review):** confirm the retirement doesn't strand a value customized in the window between Task 3's seed and this retirement — on any real deployment both run in one release, so state in the report that no production deployment sees a customizable-then-retired window.
6. **Page N of M** already ON (the quote's nature). **Overflow investigate-first** for continuation identity (many lines → the quote number band).

## Tests (TDD; RED evidence REQUIRED)

Golden: `tests/quote-pdf.test.ts` — the FOOTER exemption is removed and the builder joins purity; the rest of the suite's content assertions stay green (the printed page is identical). The Phase 6 quote suites untouched. Config-driven: label/width/font/format overrides; the two-money-precisions pin (the load-bearing one); §5.6 shapes; resolution + stamp through the real path; #97 the guard both directions. Settings retirement: the four keys gone from the registry (a read throws/absent), the seeds still carry the values, the migration deletes the rows (drift guard / a test that the `Setting` rows are gone post-migration on the test DB). No quote text reads a Setting anymore (grep-level assertion or a test).

## Gates — E2E REQUIRED

Four unit gates + full E2E **detached, sentinel `e2e-task14.done`**, `build` after E2E. `migrate status` clean on BOTH DBs (a migration lands this task). Rows from the run's own output or PENDING; dev-DB fixtures cleared. Commit in small logical units.

## Report

`docs/execution/2026-08-12-phase-7-template-designer/task-14-report.md`: the two-money-precisions mapping, the footer-retirement + purity-join, #97, the settings-retirement + the stranded-Setting finding, the overflow finding, RED evidence, all five gates + migrate status watched, deviations, notes for the UI tasks (15–21). **This is the last conversion — note in the report that all eight document types are now config-consumers and the render/contract/service infra is fully exercised.** Final message: 5-line summary + report path. Update your ledger row.
