# T1 — SetupState singleton migration + schema

**Goal:** the one new 8B table lands; the Prisma client regenerates; the test DB never runs in a state prod cannot be in.

**Scope (plan §T1):**
- `prisma/schema.prisma`: add `model SetupState` cloning the `BillingConfig` singleton — `id String @id @default("singleton")`, `numbersConfirmedAt DateTime?`, `checklistDismissedAt DateTime?`, `updatedAt DateTime @updatedAt`. No FKs. **Exactly two non-derivable facts** (the §5.7 password reminder is a live signal + client-side dismiss — no `passwordReminderDismissedAt`).
- Hand-write `prisma/migrations/<ts>_setup_state/migration.sql` via the TTY-less workflow: `CREATE TABLE` + PK, `ALTER TABLE … ADD CONSTRAINT "SetupState_singleton_check" CHECK ("id" = 'singleton')`, and `INSERT … ('singleton', now()) ON CONFLICT DO NOTHING` (migrate diff emits neither the CHECK nor the seed — add by hand, mirroring `20260806221500_pricing_and_invoicing` lines 188/352/356).
- Apply to dev `erp` **and** `erp_test` via two `migrate deploy` calls; `prisma generate`.
- `tests/helpers/db.ts`: add the `SetupState` `INSERT … ON CONFLICT DO NOTHING` re-seed line beside `BillingConfig`'s in `truncateAll`.
- **NOT here:** the `AuditableModel`/`SNAPSHOT_INCLUDE` edit and the service (→ T4 — the exhaustive Record fails tsc before the client carries the model).

**Test-first:** after `migrate deploy`, `prisma.setupState.findFirst({ where: { id: 'singleton' } })` returns the migration-seeded row; a second row with `id !== 'singleton'` is rejected by the CHECK; the row survives `truncateAll()`.

**Acceptance:** both DBs on the new migration; `prisma migrate status` clean; client exposes `setupState`; full gate chain green.

**Review focus:** the CHECK + seed are hand-added (not lost by `migrate diff`); migration applied to BOTH DBs; no premature audit-union edit.

**Also (house rule):** this task commits the execution record (`docs/execution/2026-08-15-phase-8b/`) and the plan doc.
