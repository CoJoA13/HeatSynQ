# T2 — practiceMode() db-identity leaf

**Goal:** the single source of practice-vs-production every consumer reads (§5.1).

**Scope:** new dependency-free leaf `src/server/practice-mode.ts` (order-locks.ts/invoice-guards.ts precedent — imports only db types, `prisma`, and the zero-import `errors.ts`):
- `PRACTICE_DB_NAME = "erp_practice"`.
- `practiceMode(): Promise<boolean>` — **memoized** (module-level lazy promise) `SELECT current_database() = 'erp_practice'`; process-constant (§5.1 forbids per-request DB switching). Throws loudly if `PRACTICE_MODE=true` but the DB is not `erp_practice` (the dangerous direction).
- `assertPracticeDatabase(db=prisma): Promise<void>` — un-memoized in-request re-check, `HttpError(403)` unless `erp_practice` (the §5.3 reset guard).

**Test-first (RED, no stubbing):** `practiceMode()` resolves false in the `erp_test` process; `assertPracticeDatabase(prisma)` rejects with `HttpError` status 403.

**Commit:** `d222daa`.

**Review focus:** memoization present; loud throw on the dangerous env mismatch only (reverse is safe); leaf drags no service/permission graph; db-identity authoritative.
