# T3 — order-entry readiness predicate leaf

**Goal:** the two BLOCKING install signals in one leaf so the gate (T7), the UI notice (T8), and the rollup (T9) share one source and `orders.ts` grows no import cycle (§5.6).

**Scope:** new leaf `src/server/order-entry-readiness.ts`:
- `SetupGap = { label, href }` (reuse only the `ReadinessGap` shape, NOT gl-mapping's `kind` union).
- `orderEntryReadiness(db=prisma): Promise<{ ready, gaps }>` — company identity = `company_name/address/phone` all `.trim() !== ""`; chart of accounts = `glAccount.count({deletedAt:null}) > 0` AND `getBillingConfig().arGlAccountId != null`. Threadable on a caller's client. Pure read (no claim/audit/Serializable).
- hrefs: identity → `/admin/settings`, GL accounts → `/admin/reference`, A/R account → `/admin/billing`.

**Test-first:** company-identity gap when a company field blank; no-GL + A/R gaps when chart unset; A/R gap only when GL accounts exist but arGl unset; ready with empty gaps when both set.

**Commit:** `410b279`.

**Review focus:** own signals (does not extend `readinessGaps`); pure read; the `deletedAt:null` live filter; the three hrefs are real admin routes.
